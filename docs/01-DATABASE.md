# 01 — DATABASE

Generated by reading the live Postgres schema of the project's Lovable Cloud
(Supabase) database plus the application code in `src/` and
`supabase/functions/`. Every constraint, policy, function body and trigger
below is reproduced verbatim from the catalogue (`pg_constraint`,
`pg_policies`, `pg_proc`, `pg_trigger`, `information_schema.columns`).

Column `Default` and constraint text are printed exactly as stored.

---

## 1. Table inventory

Total tables in schema `public`: **65**.

### `abandoned_carts`

**Purpose:** Checkout sessions that were started but never converted into a booking; used by the abandoned-cart admin view (src/hooks/use-abandoned-carts.ts).

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `user_id` | uuid | yes | — |
| `session_id` | text | no | — |
| `email` | text | yes | — |
| `phone` | text | yes | — |
| `first_name` | text | yes | — |
| `last_name` | text | yes | — |
| `vehicle_id` | uuid | yes | — |
| `pickup_date` | timestamp with time zone | yes | — |
| `return_date` | timestamp with time zone | yes | — |
| `location_id` | uuid | yes | — |
| `delivery_mode` | text | yes | — |
| `delivery_address` | text | yes | — |
| `protection` | text | yes | — |
| `add_on_ids` | _text | yes | — |
| `total_amount` | numeric | yes | — |
| `cart_data` | jsonb | yes | `'{}'::jsonb` |
| `abandoned_at` | timestamp with time zone | no | `now()` |
| `converted_at` | timestamp with time zone | yes | — |
| `contacted_at` | timestamp with time zone | yes | — |
| `contact_notes` | text | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `abandoned_carts_location_id_fkey` | FOREIGN KEY (location_id) REFERENCES locations(id) |
| `abandoned_carts_vehicle_id_fkey` | FOREIGN KEY (vehicle_id) REFERENCES vehicles(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `abandoned_carts_pkey`: `CREATE UNIQUE INDEX abandoned_carts_pkey ON public.abandoned_carts USING btree (id)`
- `idx_abandoned_carts_abandoned_at`: `CREATE INDEX idx_abandoned_carts_abandoned_at ON public.abandoned_carts USING btree (abandoned_at)`
- `idx_abandoned_carts_email`: `CREATE INDEX idx_abandoned_carts_email ON public.abandoned_carts USING btree (email)`
- `idx_abandoned_carts_phone`: `CREATE INDEX idx_abandoned_carts_phone ON public.abandoned_carts USING btree (phone)`
- `idx_abandoned_carts_session`: `CREATE INDEX idx_abandoned_carts_session ON public.abandoned_carts USING btree (session_id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `add_ons`

**Purpose:** Catalogue of purchasable rental extras and their daily/one-time prices (src/hooks/use-add-ons.ts).

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `name` | text | no | — |
| `description` | text | yes | — |
| `daily_rate` | numeric(10,2) | no | — |
| `one_time_fee` | numeric(10,2) | yes | `0` |
| `is_active` | boolean | yes | `true` |
| `created_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:** none

**Indexes:**

- `add_ons_pkey`: `CREATE UNIQUE INDEX add_ons_pkey ON public.add_ons USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `admin_alerts`

**Purpose:** Operational alert feed shown on the admin Alerts board (src/hooks/use-alerts.ts).

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | yes | — |
| `vehicle_id` | uuid | yes | — |
| `user_id` | uuid | yes | — |
| `alert_type` | alert_type | no | — |
| `title` | text | no | — |
| `message` | text | yes | — |
| `status` | alert_status | no | `'pending'::alert_status` |
| `acknowledged_by` | uuid | yes | — |
| `acknowledged_at` | timestamp with time zone | yes | — |
| `resolved_by` | uuid | yes | — |
| `resolved_at` | timestamp with time zone | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `expires_at` | timestamp with time zone | yes | — |
| `ticket_id` | uuid | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `admin_alerts_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) |
| `admin_alerts_ticket_id_fkey` | FOREIGN KEY (ticket_id) REFERENCES support_tickets_v2(id) ON DELETE SET NULL |
| `admin_alerts_vehicle_id_fkey` | FOREIGN KEY (vehicle_id) REFERENCES vehicles(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `admin_alerts_pkey`: `CREATE UNIQUE INDEX admin_alerts_pkey ON public.admin_alerts USING btree (id)`
- `admin_alerts_ticket_id_idx`: `CREATE INDEX admin_alerts_ticket_id_idx ON public.admin_alerts USING btree (ticket_id)`
- `idx_alerts_status`: `CREATE INDEX idx_alerts_status ON public.admin_alerts USING btree (status)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `analytics_events`

**Purpose:** Raw funnel/analytics event stream written by the client (src/hooks/use-analytics-events.ts).

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `event` | text | no | — |
| `properties` | jsonb | yes | `'{}'::jsonb` |
| `page` | text | yes | — |
| `session_id` | text | yes | — |
| `user_id` | uuid | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:** none

**Indexes:**

- `analytics_events_pkey`: `CREATE UNIQUE INDEX analytics_events_pkey ON public.analytics_events USING btree (id)`
- `idx_analytics_events_created_at`: `CREATE INDEX idx_analytics_events_created_at ON public.analytics_events USING btree (created_at DESC)`
- `idx_analytics_events_event`: `CREATE INDEX idx_analytics_events_event ON public.analytics_events USING btree (event)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `audit_logs`

**Purpose:** Append-only audit trail of privileged mutations, written mostly by edge functions.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `user_id` | uuid | yes | — |
| `action` | text | no | — |
| `entity_type` | text | no | — |
| `entity_id` | uuid | yes | — |
| `old_data` | jsonb | yes | — |
| `new_data` | jsonb | yes | — |
| `ip_address` | text | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `location_id` | uuid | yes | — |
| `actor_role` | text | yes | — |
| `actor_location_id` | uuid | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:** none

**Indexes:**

- `audit_logs_pkey`: `CREATE UNIQUE INDEX audit_logs_pkey ON public.audit_logs USING btree (id)`
- `idx_audit_logs_location_id`: `CREATE INDEX idx_audit_logs_location_id ON public.audit_logs USING btree (location_id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `bank_transfer_otps`

**Purpose:** One-time codes for confirming offline/bank-transfer payment (supabase/functions/send-bank-transfer-otp).

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `requested_by` | uuid | no | — |
| `otp_hash` | text | no | — |
| `expires_at` | timestamp with time zone | no | — |
| `verified_at` | timestamp with time zone | yes | — |
| `attempts` | integer | no | `0` |
| `created_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `bank_transfer_otps_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `bank_transfer_otps_pkey`: `CREATE UNIQUE INDEX bank_transfer_otps_pkey ON public.bank_transfer_otps USING btree (id)`
- `idx_bank_transfer_otps_booking`: `CREATE INDEX idx_bank_transfer_otps_booking ON public.bank_transfer_otps USING btree (booking_id)`
- `idx_bank_transfer_otps_expires`: `CREATE INDEX idx_bank_transfer_otps_expires ON public.bank_transfer_otps USING btree (expires_at)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `booking_access_tokens`

**Purpose:** Hashed short-lived tokens letting a guest reach their own booking without an account (supabase/functions/verify-booking-otp).

**RLS enabled:** yes · **FORCE RLS:** yes

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `token_hash` | text | no | — |
| `expires_at` | timestamp with time zone | no | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `used_at` | timestamp with time zone | yes | — |
| `revoked_at` | timestamp with time zone | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `booking_access_tokens_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Unique constraints:**

- `booking_access_tokens_booking_token_unique`: UNIQUE (booking_id, token_hash)

**Indexes:**

- `booking_access_tokens_booking_token_unique`: `CREATE UNIQUE INDEX booking_access_tokens_booking_token_unique ON public.booking_access_tokens USING btree (booking_id, token_hash)`
- `booking_access_tokens_pkey`: `CREATE UNIQUE INDEX booking_access_tokens_pkey ON public.booking_access_tokens USING btree (id)`
- `idx_booking_access_tokens_booking_expires`: `CREATE INDEX idx_booking_access_tokens_booking_expires ON public.booking_access_tokens USING btree (booking_id, expires_at)`
- `idx_booking_access_tokens_booking_id`: `CREATE INDEX idx_booking_access_tokens_booking_id ON public.booking_access_tokens USING btree (booking_id)`
- `idx_booking_access_tokens_expires_at`: `CREATE INDEX idx_booking_access_tokens_expires_at ON public.booking_access_tokens USING btree (expires_at)`
- `idx_booking_access_tokens_token_hash`: `CREATE INDEX idx_booking_access_tokens_token_hash ON public.booking_access_tokens USING btree (token_hash)`
- `ux_booking_access_tokens_one_unrevoked`: `CREATE UNIQUE INDEX ux_booking_access_tokens_one_unrevoked ON public.booking_access_tokens USING btree (booking_id) WHERE (revoked_at IS NULL)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `booking_add_ons`

**Purpose:** Join table: add-ons purchased on a booking, with server-computed price.

**RLS enabled:** yes · **FORCE RLS:** yes

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `add_on_id` | uuid | no | — |
| `quantity` | integer | yes | `1` |
| `price` | numeric(10,2) | no | — |
| `created_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `booking_add_ons_add_on_id_fkey` | FOREIGN KEY (add_on_id) REFERENCES add_ons(id) |
| `booking_add_ons_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `booking_add_ons_pkey`: `CREATE UNIQUE INDEX booking_add_ons_pkey ON public.booking_add_ons USING btree (id)`
- `idx_booking_add_ons_booking_id`: `CREATE INDEX idx_booking_add_ons_booking_id ON public.booking_add_ons USING btree (booking_id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `booking_additional_drivers`

**Purpose:** Additional drivers authorised on a booking plus their young-driver fee.

**RLS enabled:** yes · **FORCE RLS:** yes

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `driver_name` | text | yes | — |
| `driver_age_band` | text | no | — |
| `young_driver_fee` | numeric | no | `0` |
| `created_at` | timestamp with time zone | no | `now()` |
| `driver_license_number` | text | yes | — |
| `driver_license_expiry` | date | yes | — |
| `authorized_start` | timestamp with time zone | yes | — |
| `authorized_end` | timestamp with time zone | yes | — |
| `authorized_days` | integer | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `booking_additional_drivers_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**CHECK constraints (values written out in full):**

- `booking_additional_drivers_driver_age_band_check`: `CHECK ((driver_age_band = ANY (ARRAY['20_24'::text, '21_25'::text, '25_70'::text])))`

**Indexes:**

- `booking_additional_drivers_pkey`: `CREATE UNIQUE INDEX booking_additional_drivers_pkey ON public.booking_additional_drivers USING btree (id)`
- `idx_booking_additional_drivers_booking_id`: `CREATE INDEX idx_booking_additional_drivers_booking_id ON public.booking_additional_drivers USING btree (booking_id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `booking_documents`

**Purpose:** Documents uploaded against a booking during handover (supabase/functions/manage-booking-documents).

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `customer_user_id` | uuid | yes | — |
| `label` | text | no | — |
| `notes` | text | yes | — |
| `storage_path` | text | no | — |
| `file_name` | text | no | — |
| `mime_type` | text | yes | — |
| `file_size` | bigint | yes | — |
| `uploaded_by` | uuid | yes | — |
| `uploaded_at_location_id` | uuid | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `deleted_at` | timestamp with time zone | yes | — |
| `deleted_by` | uuid | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `booking_documents_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE CASCADE |
| `booking_documents_uploaded_at_location_id_fkey` | FOREIGN KEY (uploaded_at_location_id) REFERENCES locations(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `booking_documents_pkey`: `CREATE UNIQUE INDEX booking_documents_pkey ON public.booking_documents USING btree (id)`
- `idx_booking_documents_booking`: `CREATE INDEX idx_booking_documents_booking ON public.booking_documents USING btree (booking_id) WHERE (deleted_at IS NULL)`
- `idx_booking_documents_customer`: `CREATE INDEX idx_booking_documents_customer ON public.booking_documents USING btree (customer_user_id) WHERE (deleted_at IS NULL)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `booking_extensions`

**Purpose:** Recorded rental extensions (new end date, delta amount, linked agreement).

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `previous_end_at` | timestamp with time zone | yes | — |
| `new_end_at` | timestamp with time zone | yes | — |
| `odometer_km` | integer | yes | — |
| `previous_odometer_km` | integer | yes | — |
| `reason` | text | yes | — |
| `price_difference` | numeric | yes | `0` |
| `recorded_by` | uuid | yes | — |
| `agreement_id` | uuid | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `booking_extensions_agreement_id_fkey` | FOREIGN KEY (agreement_id) REFERENCES rental_agreements(id) ON DELETE SET NULL |
| `booking_extensions_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `booking_extensions_pkey`: `CREATE UNIQUE INDEX booking_extensions_pkey ON public.booking_extensions USING btree (id)`
- `idx_booking_extensions_booking_id`: `CREATE INDEX idx_booking_extensions_booking_id ON public.booking_extensions USING btree (booking_id, created_at DESC)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `booking_otps`

**Purpose:** One-time codes issued for guest booking verification (supabase/functions/send-booking-otp).

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `user_id` | uuid | no | — |
| `otp_hash` | text | no | — |
| `channel` | text | no | — |
| `expires_at` | timestamp with time zone | no | — |
| `verified_at` | timestamp with time zone | yes | — |
| `attempts` | integer | yes | `0` |
| `created_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `booking_otps_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**CHECK constraints (values written out in full):**

- `booking_otps_channel_check`: `CHECK ((channel = ANY (ARRAY['sms'::text, 'email'::text])))`

**Indexes:**

- `booking_otps_pkey`: `CREATE UNIQUE INDEX booking_otps_pkey ON public.booking_otps USING btree (id)`
- `idx_booking_otps_booking_id`: `CREATE INDEX idx_booking_otps_booking_id ON public.booking_otps USING btree (booking_id)`
- `idx_booking_otps_expires_at`: `CREATE INDEX idx_booking_otps_expires_at ON public.booking_otps USING btree (expires_at)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `bookings`

**Purpose:** Central rental record: customer, vehicle/category, dates, location, full pricing snapshot, status, deposit state, return/closeout state.

**RLS enabled:** yes · **FORCE RLS:** yes

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_code` | text | no | — |
| `user_id` | uuid | no | — |
| `vehicle_id` | uuid | no | — |
| `location_id` | uuid | no | — |
| `start_at` | timestamp with time zone | no | — |
| `end_at` | timestamp with time zone | no | — |
| `actual_return_at` | timestamp with time zone | yes | — |
| `status` | booking_status | no | `'pending'::booking_status` |
| `daily_rate` | numeric(10,2) | no | — |
| `total_days` | integer | no | — |
| `subtotal` | numeric(10,2) | no | — |
| `tax_amount` | numeric(10,2) | yes | `0` |
| `deposit_amount` | numeric(10,2) | yes | `0` |
| `total_amount` | numeric(10,2) | no | — |
| `pickup_address` | text | yes | — |
| `pickup_place_id` | text | yes | — |
| `pickup_lat` | numeric(10,8) | yes | — |
| `pickup_lng` | numeric(11,8) | yes | — |
| `notes` | text | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |
| `assigned_driver_id` | uuid | yes | — |
| `driver_age_band` | text | yes | — |
| `young_driver_fee` | numeric | yes | `0` |
| `assigned_unit_id` | uuid | yes | — |
| `save_time_at_counter` | boolean | yes | `false` |
| `pickup_contact_name` | text | yes | — |
| `pickup_contact_phone` | text | yes | — |
| `special_instructions` | text | yes | — |
| `return_state` | text | yes | `'not_started'::text` |
| `return_started_at` | timestamp with time zone | yes | — |
| `return_intake_completed_at` | timestamp with time zone | yes | — |
| `return_intake_completed_by` | uuid | yes | — |
| `return_evidence_completed_at` | timestamp with time zone | yes | — |
| `return_evidence_completed_by` | uuid | yes | — |
| `return_issues_reviewed_at` | timestamp with time zone | yes | — |
| `return_issues_reviewed_by` | uuid | yes | — |
| `return_is_exception` | boolean | yes | `false` |
| `return_exception_reason` | text | yes | — |
| `handed_over_at` | timestamp with time zone | yes | — |
| `handed_over_by` | uuid | yes | — |
| `handover_sms_sent_at` | timestamp with time zone | yes | — |
| `original_vehicle_id` | uuid | yes | — |
| `upgraded_at` | timestamp with time zone | yes | — |
| `upgraded_by` | uuid | yes | — |
| `upgrade_reason` | text | yes | — |
| `booking_source` | text | yes | `'online'::text` |
| `card_last_four` | character varying(4) | yes | — |
| `card_type` | character varying(20) | yes | — |
| `card_holder_name` | character varying(255) | yes | — |
| `late_return_fee` | numeric | yes | `0` |
| `late_return_fee_override` | numeric | yes | — |
| `late_return_override_reason` | text | yes | — |
| `late_return_override_by` | uuid | yes | — |
| `late_return_override_at` | timestamp with time zone | yes | — |
| `customer_marked_returned_at` | timestamp with time zone | yes | — |
| `deposit_status` | text | yes | `'none'::text` |
| `deposit_authorized_at` | timestamp with time zone | yes | — |
| `deposit_captured_at` | timestamp with time zone | yes | — |
| `deposit_released_at` | timestamp with time zone | yes | — |
| `deposit_expires_at` | timestamp with time zone | yes | — |
| `deposit_captured_amount` | integer | yes | — |
| `deposit_capture_reason` | text | yes | — |
| `final_invoice_generated` | boolean | yes | `false` |
| `final_invoice_id` | text | yes | — |
| `account_closed_at` | timestamp with time zone | yes | — |
| `account_closed_by` | uuid | yes | — |
| `customer_return_lat` | numeric(10,8) | yes | — |
| `customer_return_lng` | numeric(11,8) | yes | — |
| `customer_return_address` | text | yes | — |
| `protection_plan` | text | yes | `'none'::text` |
| `pricing_snapshot` | jsonb | yes | — |
| `pricing_locked_at` | timestamp with time zone | yes | — |
| `pricing_locked_by` | uuid | yes | — |
| `activated_at` | timestamp with time zone | yes | — |
| `activated_by` | uuid | yes | — |
| `activation_source` | text | yes | — |
| `activation_reason` | text | yes | — |
| `return_location_id` | uuid | yes | — |
| `different_dropoff_fee` | numeric | yes | `0` |
| `upgrade_daily_fee` | numeric | yes | `0` |
| `upgrade_category_label` | text | yes | — |
| `upgrade_visible_to_customer` | boolean | yes | `false` |
| `internal_unit_category_id` | uuid | yes | — |
| `delivery_fee` | numeric | yes | `0` |
| `wl_transaction_id` | text | yes | — |
| `wl_auth_status` | text | yes | — |
| `wl_profile_customer_code` | text | yes | — |
| `wl_deposit_transaction_id` | text | yes | — |
| `wl_deposit_auth_status` | text | yes | — |
| `customer_id` | uuid | yes | — |
| `created_by` | uuid | yes | — |
| `paid_offline` | boolean | no | `false` |
| `offline_payment_method` | text | yes | — |
| `offline_payment_reference` | text | yes | — |
| `offline_paid_at` | timestamp with time zone | yes | — |
| `offline_paid_by` | uuid | yes | — |
| `weekend_surcharge` | numeric | no | `0` |
| `duration_discount` | numeric | no | `0` |
| `overbooked` | boolean | no | `false` |
| `overbooked_at` | timestamp with time zone | yes | — |
| `processing_fee` | numeric | no | `0` |
| `processing_fee_rate` | numeric | no | `0` |
| `card_expiry` | text | yes | — |
| `processed_by` | uuid | yes | — |
| `processed_at` | timestamp with time zone | yes | — |
| `processed_at_location_id` | uuid | yes | — |
| `closed_by` | uuid | yes | — |
| `last_modified_by` | uuid | yes | — |
| `promo_code` | text | yes | — |
| `promo_discount` | numeric | no | `0` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `bookings_account_closed_by_fkey` | FOREIGN KEY (account_closed_by) REFERENCES auth.users(id) |
| `bookings_assigned_driver_id_fkey` | FOREIGN KEY (assigned_driver_id) REFERENCES auth.users(id) |
| `bookings_assigned_unit_id_fkey` | FOREIGN KEY (assigned_unit_id) REFERENCES vehicle_units(id) |
| `bookings_created_by_fkey` | FOREIGN KEY (created_by) REFERENCES auth.users(id) |
| `bookings_customer_id_fkey` | FOREIGN KEY (customer_id) REFERENCES customers(id) |
| `bookings_internal_unit_category_id_fkey` | FOREIGN KEY (internal_unit_category_id) REFERENCES vehicle_categories(id) |
| `bookings_location_id_fkey` | FOREIGN KEY (location_id) REFERENCES locations(id) |
| `bookings_return_location_id_fkey` | FOREIGN KEY (return_location_id) REFERENCES locations(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Unique constraints:**

- `bookings_booking_code_key`: UNIQUE (booking_code)

**CHECK constraints (values written out in full):**

- `bookings_deposit_status_check`: `CHECK ((deposit_status = ANY (ARRAY['none'::text, 'requires_payment'::text, 'authorizing'::text, 'authorized'::text, 'capturing'::text, 'captured'::text, 'releasing'::text, 'released'::text, 'failed'::text, 'expired'::text, 'canceled'::text])))`
- `bookings_driver_age_band_check`: `CHECK ((driver_age_band = ANY (ARRAY['20_24'::text, '25_70'::text])))`
- `check_return_state`: `CHECK ((return_state = ANY (ARRAY['not_started'::text, 'initiated'::text, 'intake_done'::text, 'evidence_done'::text, 'issues_reviewed'::text, 'closeout_done'::text, 'deposit_processed'::text])))`
- `walkin_requires_customer_id`: `CHECK (((booking_source <> 'walk_in'::text) OR (customer_id IS NOT NULL)))`

**Indexes:**

- `bookings_booking_code_key`: `CREATE UNIQUE INDEX bookings_booking_code_key ON public.bookings USING btree (booking_code)`
- `bookings_pkey`: `CREATE UNIQUE INDEX bookings_pkey ON public.bookings USING btree (id)`
- `bookings_promo_code_idx`: `CREATE INDEX bookings_promo_code_idx ON public.bookings USING btree (promo_code) WHERE (promo_code IS NOT NULL)`
- `idx_bookings_assigned_unit_id`: `CREATE INDEX idx_bookings_assigned_unit_id ON public.bookings USING btree (assigned_unit_id)`
- `idx_bookings_card_last_four`: `CREATE INDEX idx_bookings_card_last_four ON public.bookings USING btree (card_last_four) WHERE (card_last_four IS NOT NULL)`
- `idx_bookings_code`: `CREATE INDEX idx_bookings_code ON public.bookings USING btree (booking_code)`
- `idx_bookings_created_at`: `CREATE INDEX idx_bookings_created_at ON public.bookings USING btree (created_at)`
- `idx_bookings_dates`: `CREATE INDEX idx_bookings_dates ON public.bookings USING btree (start_at, end_at)`
- `idx_bookings_deposit_expires`: `CREATE INDEX idx_bookings_deposit_expires ON public.bookings USING btree (deposit_expires_at) WHERE (deposit_status = 'authorized'::text)`
- `idx_bookings_deposit_status`: `CREATE INDEX idx_bookings_deposit_status ON public.bookings USING btree (deposit_status) WHERE (deposit_status <> 'none'::text)`
- `idx_bookings_end_at`: `CREATE INDEX idx_bookings_end_at ON public.bookings USING btree (end_at)`
- `idx_bookings_handed_over_at`: `CREATE INDEX idx_bookings_handed_over_at ON public.bookings USING btree (handed_over_at)`
- `idx_bookings_location_id`: `CREATE INDEX idx_bookings_location_id ON public.bookings USING btree (location_id)`
- `idx_bookings_processed_by`: `CREATE INDEX idx_bookings_processed_by ON public.bookings USING btree (processed_by)`
- `idx_bookings_return_location_id`: `CREATE INDEX idx_bookings_return_location_id ON public.bookings USING btree (return_location_id)`
- `idx_bookings_return_state`: `CREATE INDEX idx_bookings_return_state ON public.bookings USING btree (return_state) WHERE (status = 'active'::booking_status)`
- `idx_bookings_source`: `CREATE INDEX idx_bookings_source ON public.bookings USING btree (booking_source)`
- `idx_bookings_start_at`: `CREATE INDEX idx_bookings_start_at ON public.bookings USING btree (start_at)`
- `idx_bookings_status`: `CREATE INDEX idx_bookings_status ON public.bookings USING btree (status)`
- `idx_bookings_status_created`: `CREATE INDEX idx_bookings_status_created ON public.bookings USING btree (status, created_at)`
- `idx_bookings_upgraded`: `CREATE INDEX idx_bookings_upgraded ON public.bookings USING btree (upgraded_at) WHERE (upgraded_at IS NOT NULL)`
- `idx_bookings_user`: `CREATE INDEX idx_bookings_user ON public.bookings USING btree (user_id)`
- `idx_bookings_vehicle`: `CREATE INDEX idx_bookings_vehicle ON public.bookings USING btree (vehicle_id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `checkin_records`

**Purpose:** Handover check-in capture (fuel, odometer, signatures) per booking.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `identity_verified` | boolean | yes | `false` |
| `identity_notes` | text | yes | — |
| `license_verified` | boolean | yes | `false` |
| `license_name_matches` | boolean | yes | `false` |
| `license_valid` | boolean | yes | `false` |
| `license_expiry_date` | date | yes | — |
| `license_notes` | text | yes | — |
| `age_verified` | boolean | yes | `false` |
| `customer_dob` | date | yes | — |
| `age_notes` | text | yes | — |
| `arrival_time` | timestamp with time zone | yes | — |
| `timing_status` | text | yes | — |
| `timing_notes` | text | yes | — |
| `check_in_status` | text | no | `'pending'::text` |
| `blocked_reason` | text | yes | — |
| `checked_in_by` | uuid | yes | — |
| `checked_in_at` | timestamp with time zone | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `checkin_records_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Unique constraints:**

- `checkin_records_booking_id_key`: UNIQUE (booking_id)

**CHECK constraints (values written out in full):**

- `checkin_records_check_in_status_check`: `CHECK ((check_in_status = ANY (ARRAY['pending'::text, 'passed'::text, 'needs_review'::text, 'blocked'::text])))`
- `checkin_records_timing_status_check`: `CHECK ((timing_status = ANY (ARRAY['on_time'::text, 'early'::text, 'late'::text, 'no_show'::text])))`

**Indexes:**

- `checkin_records_booking_id_key`: `CREATE UNIQUE INDEX checkin_records_booking_id_key ON public.checkin_records USING btree (booking_id)`
- `checkin_records_pkey`: `CREATE UNIQUE INDEX checkin_records_pkey ON public.checkin_records USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `competitor_pricing`

**Purpose:** Manually entered competitor rates used by pricing analytics.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `vehicle_id` | uuid | no | — |
| `competitor_name` | text | no | — |
| `daily_rate` | numeric | yes | — |
| `weekly_rate` | numeric | yes | — |
| `monthly_rate` | numeric | yes | — |
| `notes` | text | yes | — |
| `last_updated` | date | yes | `CURRENT_DATE` |
| `updated_by` | uuid | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `competitor_pricing_vehicle_id_fkey` | FOREIGN KEY (vehicle_id) REFERENCES vehicles(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `competitor_pricing_pkey`: `CREATE UNIQUE INDEX competitor_pricing_pkey ON public.competitor_pricing USING btree (id)`
- `idx_competitor_pricing_vehicle_id`: `CREATE INDEX idx_competitor_pricing_vehicle_id ON public.competitor_pricing USING btree (vehicle_id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `condition_photos`

**Purpose:** Vehicle condition photos captured at pickup/return.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `phase` | text | no | — |
| `photo_type` | text | no | — |
| `photo_url` | text | no | — |
| `captured_by` | uuid | no | — |
| `captured_at` | timestamp with time zone | no | `now()` |
| `notes` | text | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `condition_photos_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**CHECK constraints (values written out in full):**

- `condition_photos_phase_check`: `CHECK ((phase = ANY (ARRAY['pickup'::text, 'return'::text])))`

**Indexes:**

- `condition_photos_pkey`: `CREATE UNIQUE INDEX condition_photos_pkey ON public.condition_photos USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `customers`

**Purpose:** Real-world customer identity, decoupled from auth users/profiles.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `full_name` | text | no | — |
| `email` | text | yes | — |
| `phone` | text | yes | — |
| `notes` | text | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:** none

**Indexes:**

- `customers_pkey`: `CREATE UNIQUE INDEX customers_pkey ON public.customers USING btree (id)`
- `idx_customers_email`: `CREATE UNIQUE INDEX idx_customers_email ON public.customers USING btree (lower(email)) WHERE (email IS NOT NULL)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `damage_reports`

**Purpose:** Damage found on a vehicle, tied to a booking; auto-creates a support ticket.

**RLS enabled:** yes · **FORCE RLS:** yes

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `vehicle_id` | uuid | no | — |
| `reported_by` | uuid | no | — |
| `severity` | damage_severity | no | — |
| `location_on_vehicle` | text | no | — |
| `description` | text | no | — |
| `photo_urls` | jsonb | yes | `'[]'::jsonb` |
| `estimated_cost` | numeric(10,2) | yes | — |
| `status` | text | no | `'under_review'::text` |
| `resolution_notes` | text | yes | — |
| `resolved_at` | timestamp with time zone | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `vehicle_unit_id` | uuid | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `damage_reports_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) |
| `damage_reports_vehicle_id_fkey` | FOREIGN KEY (vehicle_id) REFERENCES vehicle_categories(id) |
| `damage_reports_vehicle_unit_id_fkey` | FOREIGN KEY (vehicle_unit_id) REFERENCES vehicle_units(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `damage_reports_pkey`: `CREATE UNIQUE INDEX damage_reports_pkey ON public.damage_reports USING btree (id)`
- `idx_damage_reports_vehicle_unit_id`: `CREATE INDEX idx_damage_reports_vehicle_unit_id ON public.damage_reports USING btree (vehicle_unit_id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `delivery_status_log`

**Purpose:** History of delivery status transitions for delivery bookings.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `status` | text | no | — |
| `notes` | text | yes | — |
| `photo_urls` | jsonb | yes | — |
| `location_lat` | numeric | yes | — |
| `location_lng` | numeric | yes | — |
| `odometer_reading` | integer | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `created_by` | uuid | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `delivery_status_log_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE CASCADE |
| `delivery_status_log_created_by_fkey` | FOREIGN KEY (created_by) REFERENCES auth.users(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `delivery_status_log_pkey`: `CREATE UNIQUE INDEX delivery_status_log_pkey ON public.delivery_status_log USING btree (id)`
- `idx_delivery_status_log_booking`: `CREATE INDEX idx_delivery_status_log_booking ON public.delivery_status_log USING btree (booking_id)`
- `idx_delivery_status_log_created`: `CREATE INDEX idx_delivery_status_log_created ON public.delivery_status_log USING btree (created_at DESC)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `delivery_statuses`

**Purpose:** Current delivery state per delivery booking.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `status` | text | no | `'assigned'::text` |
| `notes` | text | yes | — |
| `photo_urls` | jsonb | yes | `'[]'::jsonb` |
| `location_lat` | numeric | yes | — |
| `location_lng` | numeric | yes | — |
| `updated_by` | uuid | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `delivery_statuses_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Unique constraints:**

- `delivery_statuses_booking_id_unique`: UNIQUE (booking_id)

**CHECK constraints (values written out in full):**

- `delivery_status_check`: `CHECK ((status = ANY (ARRAY['unassigned'::text, 'assigned'::text, 'picked_up'::text, 'en_route'::text, 'arrived'::text, 'delivered'::text, 'issue'::text, 'cancelled'::text])))`

**Indexes:**

- `delivery_statuses_booking_id_unique`: `CREATE UNIQUE INDEX delivery_statuses_booking_id_unique ON public.delivery_statuses USING btree (booking_id)`
- `delivery_statuses_pkey`: `CREATE UNIQUE INDEX delivery_statuses_pkey ON public.delivery_statuses USING btree (id)`
- `idx_delivery_statuses_booking_id`: `CREATE INDEX idx_delivery_statuses_booking_id ON public.delivery_statuses USING btree (booking_id)`
- `idx_delivery_statuses_status`: `CREATE INDEX idx_delivery_statuses_status ON public.delivery_statuses USING btree (status)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `delivery_tasks`

**Purpose:** Driver-facing delivery/collection tasks.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `status` | text | no | `'pending'::text` |
| `assigned_driver_id` | uuid | yes | — |
| `intake_completed_at` | timestamp with time zone | yes | — |
| `intake_completed_by` | uuid | yes | — |
| `payment_verified_at` | timestamp with time zone | yes | — |
| `payment_verified_by` | uuid | yes | — |
| `ready_line_completed_at` | timestamp with time zone | yes | — |
| `ready_line_completed_by` | uuid | yes | — |
| `dispatched_at` | timestamp with time zone | yes | — |
| `dispatched_by` | uuid | yes | — |
| `dispatch_window_start` | timestamp with time zone | yes | — |
| `dispatch_window_end` | timestamp with time zone | yes | — |
| `driver_picked_up_at` | timestamp with time zone | yes | — |
| `driver_en_route_at` | timestamp with time zone | yes | — |
| `driver_arrived_at` | timestamp with time zone | yes | — |
| `handover_completed_at` | timestamp with time zone | yes | — |
| `handover_completed_by` | uuid | yes | — |
| `handover_photos_count` | integer | yes | `0` |
| `fuel_level_recorded` | boolean | yes | `false` |
| `odometer_recorded` | boolean | yes | `false` |
| `id_check_result` | text | yes | — |
| `id_check_required` | boolean | yes | `true` |
| `activated_at` | timestamp with time zone | yes | — |
| `activated_by` | uuid | yes | — |
| `activation_source` | text | yes | — |
| `activation_reason` | text | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `delivery_tasks_assigned_driver_id_fkey` | FOREIGN KEY (assigned_driver_id) REFERENCES auth.users(id) |
| `delivery_tasks_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Unique constraints:**

- `delivery_tasks_booking_id_key`: UNIQUE (booking_id)

**Indexes:**

- `delivery_tasks_booking_id_key`: `CREATE UNIQUE INDEX delivery_tasks_booking_id_key ON public.delivery_tasks USING btree (booking_id)`
- `delivery_tasks_pkey`: `CREATE UNIQUE INDEX delivery_tasks_pkey ON public.delivery_tasks USING btree (id)`
- `idx_delivery_tasks_booking_id`: `CREATE INDEX idx_delivery_tasks_booking_id ON public.delivery_tasks USING btree (booking_id)`
- `idx_delivery_tasks_driver`: `CREATE INDEX idx_delivery_tasks_driver ON public.delivery_tasks USING btree (assigned_driver_id) WHERE (assigned_driver_id IS NOT NULL)`
- `idx_delivery_tasks_status`: `CREATE INDEX idx_delivery_tasks_status ON public.delivery_tasks USING btree (status)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `deposit_jobs`

**Purpose:** Queued deposit operations (authorize/capture/release) to serialise deposit work.

**Database comment:** Queued deposit operations to prevent race conditions and ensure reliable processing

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `job_type` | text | no | — |
| `amount` | numeric(10,2) | no | — |
| `reason` | text | yes | — |
| `status` | text | no | `'pending'::text` |
| `attempts` | integer | no | `0` |
| `max_attempts` | integer | no | `3` |
| `last_error` | text | yes | — |
| `processed_at` | timestamp with time zone | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `deposit_jobs_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**CHECK constraints (values written out in full):**

- `deposit_jobs_job_type_check`: `CHECK ((job_type = ANY (ARRAY['release'::text, 'withhold'::text, 'partial_release'::text])))`
- `deposit_jobs_status_check`: `CHECK ((status = ANY (ARRAY['pending'::text, 'processing'::text, 'completed'::text, 'failed'::text])))`

**Indexes:**

- `deposit_jobs_pkey`: `CREATE UNIQUE INDEX deposit_jobs_pkey ON public.deposit_jobs USING btree (id)`
- `idx_deposit_jobs_booking`: `CREATE INDEX idx_deposit_jobs_booking ON public.deposit_jobs USING btree (booking_id)`
- `idx_deposit_jobs_created`: `CREATE INDEX idx_deposit_jobs_created ON public.deposit_jobs USING btree (created_at)`
- `idx_deposit_jobs_status`: `CREATE INDEX idx_deposit_jobs_status ON public.deposit_jobs USING btree (status) WHERE (status = ANY (ARRAY['pending'::text, 'processing'::text]))`
- `idx_deposit_jobs_unique_pending`: `CREATE UNIQUE INDEX idx_deposit_jobs_unique_pending ON public.deposit_jobs USING btree (booking_id, job_type) WHERE (status = ANY (ARRAY['pending'::text, 'processing'::text]))`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `deposit_ledger`

**Purpose:** Deposit hold, release and deduction history; deposits never count as revenue.

**Database comment:** Deposit hold, release, and deduction history

**RLS enabled:** yes · **FORCE RLS:** yes

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `payment_id` | uuid | yes | — |
| `action` | text | no | — |
| `amount` | numeric | no | — |
| `reason` | text | yes | — |
| `created_by` | uuid | no | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `category` | text | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `deposit_ledger_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE CASCADE |
| `deposit_ledger_payment_id_fkey` | FOREIGN KEY (payment_id) REFERENCES payments(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**CHECK constraints (values written out in full):**

- `deposit_ledger_action_check`: `CHECK ((action = ANY (ARRAY['hold'::text, 'release'::text, 'deduct'::text, 'authorize'::text, 'partial_capture'::text, 'capture'::text, 'expire'::text, 'cancel'::text])))`

**Indexes:**

- `deposit_ledger_pkey`: `CREATE UNIQUE INDEX deposit_ledger_pkey ON public.deposit_ledger USING btree (id)`
- `idx_deposit_ledger_booking_id`: `CREATE INDEX idx_deposit_ledger_booking_id ON public.deposit_ledger USING btree (booking_id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `final_invoices`

**Purpose:** Issued closing invoices with grand total, payments received and amount due.

**RLS enabled:** yes · **FORCE RLS:** yes

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `invoice_number` | text | no | — |
| `rental_subtotal` | numeric | no | `0` |
| `addons_total` | numeric | yes | `0` |
| `taxes_total` | numeric | no | `0` |
| `fees_total` | numeric | yes | `0` |
| `late_fees` | numeric | yes | `0` |
| `damage_charges` | numeric | yes | `0` |
| `deposit_held` | numeric | yes | `0` |
| `deposit_captured` | numeric | yes | `0` |
| `deposit_released` | numeric | yes | `0` |
| `payments_received` | numeric | yes | `0` |
| `grand_total` | numeric | no | `0` |
| `amount_due` | numeric | yes | `0` |
| `amount_refunded` | numeric | yes | `0` |
| `line_items_json` | jsonb | no | `'[]'::jsonb` |
| `notes` | text | yes | — |
| `status` | text | yes | `'draft'::text` |
| `issued_at` | timestamp with time zone | yes | — |
| `created_by` | uuid | yes | — |
| `created_at` | timestamp with time zone | yes | `now()` |
| `updated_at` | timestamp with time zone | yes | `now()` |
| `paid_offline_amount` | numeric | no | `0` |
| `excess_km_fee_cents` | integer | no | `0` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `final_invoices_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) |
| `final_invoices_created_by_fkey` | FOREIGN KEY (created_by) REFERENCES auth.users(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Unique constraints:**

- `final_invoices_invoice_number_key`: UNIQUE (invoice_number)

**CHECK constraints (values written out in full):**

- `final_invoices_status_check`: `CHECK ((status = ANY (ARRAY['draft'::text, 'issued'::text, 'paid'::text, 'voided'::text])))`

**Indexes:**

- `final_invoices_invoice_number_key`: `CREATE UNIQUE INDEX final_invoices_invoice_number_key ON public.final_invoices USING btree (invoice_number)`
- `final_invoices_pkey`: `CREATE UNIQUE INDEX final_invoices_pkey ON public.final_invoices USING btree (id)`
- `idx_final_invoices_booking`: `CREATE INDEX idx_final_invoices_booking ON public.final_invoices USING btree (booking_id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `fleet_cost_cache`

**Purpose:** Pre-computed per-vehicle cost aggregates for fleet analytics.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `vehicle_unit_id` | uuid | yes | — |
| `category_id` | uuid | yes | — |
| `cache_type` | text | no | — |
| `total_rental_revenue` | numeric | yes | `0` |
| `total_damage_cost` | numeric | yes | `0` |
| `total_maintenance_cost` | numeric | yes | `0` |
| `net_profit` | numeric | yes | `0` |
| `rental_count` | integer | yes | `0` |
| `total_rental_days` | integer | yes | `0` |
| `total_miles_driven` | integer | yes | `0` |
| `last_calculated_at` | timestamp with time zone | no | `now()` |
| `calculation_period_start` | date | yes | — |
| `calculation_period_end` | date | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `fleet_cost_cache_category_id_fkey` | FOREIGN KEY (category_id) REFERENCES vehicle_categories(id) ON DELETE CASCADE |
| `fleet_cost_cache_vehicle_unit_id_fkey` | FOREIGN KEY (vehicle_unit_id) REFERENCES vehicle_units(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Unique constraints:**

- `fleet_cost_cache_category_id_cache_type_calculation_period__key`: UNIQUE (category_id, cache_type, calculation_period_start, calculation_period_end)
- `fleet_cost_cache_vehicle_unit_id_cache_type_calculation_per_key`: UNIQUE (vehicle_unit_id, cache_type, calculation_period_start, calculation_period_end)

**CHECK constraints (values written out in full):**

- `fleet_cost_cache_cache_type_check`: `CHECK ((cache_type = ANY (ARRAY['unit'::text, 'category'::text])))`

**Indexes:**

- `fleet_cost_cache_category_id_cache_type_calculation_period__key`: `CREATE UNIQUE INDEX fleet_cost_cache_category_id_cache_type_calculation_period__key ON public.fleet_cost_cache USING btree (category_id, cache_type, calculation_period_start, calculation_period_end)`
- `fleet_cost_cache_pkey`: `CREATE UNIQUE INDEX fleet_cost_cache_pkey ON public.fleet_cost_cache USING btree (id)`
- `fleet_cost_cache_vehicle_unit_id_cache_type_calculation_per_key`: `CREATE UNIQUE INDEX fleet_cost_cache_vehicle_unit_id_cache_type_calculation_per_key ON public.fleet_cost_cache USING btree (vehicle_unit_id, cache_type, calculation_period_start, calculation_period_end)`
- `idx_fleet_cost_cache_category`: `CREATE INDEX idx_fleet_cost_cache_category ON public.fleet_cost_cache USING btree (category_id)`
- `idx_fleet_cost_cache_unit`: `CREATE INDEX idx_fleet_cost_cache_unit ON public.fleet_cost_cache USING btree (vehicle_unit_id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `incident_cases`

**Purpose:** Incident (accident/theft/mechanical) case records.

**RLS enabled:** yes · **FORCE RLS:** yes

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | yes | — |
| `vehicle_id` | uuid | no | — |
| `vehicle_unit_id` | uuid | yes | — |
| `customer_id` | uuid | yes | — |
| `incident_type` | text | no | — |
| `incident_date` | timestamp with time zone | no | `now()` |
| `location` | text | yes | — |
| `description` | text | no | — |
| `severity` | text | no | `'minor'::text` |
| `is_drivable` | boolean | yes | `true` |
| `towing_required` | boolean | yes | `false` |
| `airbags_deployed` | boolean | yes | `false` |
| `third_party_involved` | boolean | yes | `false` |
| `claim_number` | text | yes | — |
| `claim_required` | boolean | yes | — |
| `status` | text | no | `'reported'::text` |
| `estimate_amount` | numeric(10,2) | yes | — |
| `approved_amount` | numeric(10,2) | yes | — |
| `final_invoice_amount` | numeric(10,2) | yes | — |
| `deductible_amount` | numeric(10,2) | yes | — |
| `repair_vendor` | text | yes | — |
| `repair_vendor_contact` | text | yes | — |
| `repair_started_at` | timestamp with time zone | yes | — |
| `repair_completed_at` | timestamp with time zone | yes | — |
| `assigned_staff_id` | uuid | yes | — |
| `internal_notes` | text | yes | — |
| `evidence_complete` | boolean | yes | `false` |
| `evidence_completed_at` | timestamp with time zone | yes | — |
| `evidence_completed_by` | uuid | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |
| `closed_at` | timestamp with time zone | yes | — |
| `closed_by` | uuid | yes | — |
| `created_by` | uuid | no | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `incident_cases_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE SET NULL |
| `incident_cases_customer_id_fkey` | FOREIGN KEY (customer_id) REFERENCES auth.users(id) ON DELETE SET NULL |
| `incident_cases_vehicle_id_fkey` | FOREIGN KEY (vehicle_id) REFERENCES vehicle_categories(id) ON DELETE CASCADE |
| `incident_cases_vehicle_unit_id_fkey` | FOREIGN KEY (vehicle_unit_id) REFERENCES vehicle_units(id) ON DELETE SET NULL |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**CHECK constraints (values written out in full):**

- `incident_cases_incident_type_check`: `CHECK ((incident_type = ANY (ARRAY['accident'::text, 'collision'::text, 'damage'::text, 'customer_damage'::text, 'weather'::text, 'mechanical'::text, 'theft'::text, 'vandalism'::text, 'glass'::text, 'tire'::text, 'hit_and_run'::text, 'other'::text])))`
- `incident_cases_severity_check`: `CHECK ((severity = ANY (ARRAY['minor'::text, 'moderate'::text, 'major'::text])))`
- `incident_cases_status_check`: `CHECK ((status = ANY (ARRAY['reported'::text, 'investigating'::text, 'evidence_complete'::text, 'claim_filed'::text, 'estimate'::text, 'approved'::text, 'in_repair'::text, 'resolved'::text, 'ready'::text, 'closed'::text])))`

**Indexes:**

- `idx_incident_cases_booking`: `CREATE INDEX idx_incident_cases_booking ON public.incident_cases USING btree (booking_id)`
- `idx_incident_cases_status`: `CREATE INDEX idx_incident_cases_status ON public.incident_cases USING btree (status)`
- `idx_incident_cases_vehicle`: `CREATE INDEX idx_incident_cases_vehicle ON public.incident_cases USING btree (vehicle_id)`
- `incident_cases_pkey`: `CREATE UNIQUE INDEX incident_cases_pkey ON public.incident_cases USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `incident_photos`

**Purpose:** Photos attached to an incident case.

**RLS enabled:** yes · **FORCE RLS:** yes

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `incident_id` | uuid | no | — |
| `photo_url` | text | no | — |
| `category` | text | no | — |
| `description` | text | yes | — |
| `captured_at` | timestamp with time zone | no | `now()` |
| `uploaded_by` | uuid | no | — |
| `created_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `incident_photos_incident_id_fkey` | FOREIGN KEY (incident_id) REFERENCES incident_cases(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**CHECK constraints (values written out in full):**

- `incident_photos_category_check`: `CHECK ((category = ANY (ARRAY['scene'::text, 'vehicle_overview'::text, 'close_up'::text, 'document'::text, 'third_party'::text, 'repair'::text])))`

**Indexes:**

- `idx_incident_photos_incident`: `CREATE INDEX idx_incident_photos_incident ON public.incident_photos USING btree (incident_id)`
- `incident_photos_pkey`: `CREATE UNIQUE INDEX incident_photos_pkey ON public.incident_photos USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `incident_repairs`

**Purpose:** Repair records/costs attached to an incident case.

**RLS enabled:** yes · **FORCE RLS:** yes

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `incident_id` | uuid | no | — |
| `repair_type` | text | no | — |
| `description` | text | yes | — |
| `estimated_cost` | numeric(10,2) | yes | — |
| `actual_cost` | numeric(10,2) | yes | — |
| `vendor` | text | yes | — |
| `receipt_url` | text | yes | — |
| `status` | text | yes | `'pending'::text` |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |
| `created_by` | uuid | no | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `incident_repairs_incident_id_fkey` | FOREIGN KEY (incident_id) REFERENCES incident_cases(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**CHECK constraints (values written out in full):**

- `incident_repairs_status_check`: `CHECK ((status = ANY (ARRAY['pending'::text, 'approved'::text, 'completed'::text, 'cancelled'::text])))`

**Indexes:**

- `idx_incident_repairs_incident`: `CREATE INDEX idx_incident_repairs_incident ON public.incident_repairs USING btree (incident_id)`
- `incident_repairs_pkey`: `CREATE UNIQUE INDEX incident_repairs_pkey ON public.incident_repairs USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `inspection_metrics`

**Purpose:** Return-inspection measurements (fuel, odometer, cleanliness) per booking.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `phase` | text | no | — |
| `fuel_level` | integer | yes | — |
| `odometer` | integer | yes | — |
| `exterior_notes` | text | yes | — |
| `interior_notes` | text | yes | — |
| `recorded_by` | uuid | no | — |
| `recorded_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `inspection_metrics_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**CHECK constraints (values written out in full):**

- `inspection_metrics_phase_check`: `CHECK ((phase = ANY (ARRAY['pickup'::text, 'return'::text])))`

**Indexes:**

- `inspection_metrics_pkey`: `CREATE UNIQUE INDEX inspection_metrics_pkey ON public.inspection_metrics USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `locations`

**Purpose:** Branches customers can rent from, with address, coordinates and drop-off fee group.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `name` | text | no | — |
| `address` | text | no | — |
| `city` | text | no | — |
| `place_id` | text | yes | — |
| `lat` | numeric(10,8) | yes | — |
| `lng` | numeric(11,8) | yes | — |
| `phone` | text | yes | — |
| `email` | text | yes | — |
| `hours_json` | jsonb | yes | `'{}'::jsonb` |
| `is_active` | boolean | yes | `true` |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |
| `fee_group` | text | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:** none

**CHECK constraints (values written out in full):**

- `locations_fee_group_check`: `CHECK (((fee_group IS NULL) OR (fee_group = ANY (ARRAY['surrey'::text, 'langley'::text, 'abbotsford'::text]))))`

**Indexes:**

- `locations_pkey`: `CREATE UNIQUE INDEX locations_pkey ON public.locations USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `maintenance_logs`

**Purpose:** Vehicle maintenance events and costs.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `vehicle_unit_id` | uuid | no | — |
| `maintenance_type` | text | no | — |
| `description` | text | yes | — |
| `cost` | numeric | no | `0` |
| `mileage_at_service` | integer | yes | — |
| `service_date` | date | no | `CURRENT_DATE` |
| `vendor_name` | text | yes | — |
| `invoice_number` | text | yes | — |
| `notes` | text | yes | — |
| `receipt_url` | text | yes | — |
| `created_by` | uuid | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `maintenance_logs_created_by_fkey` | FOREIGN KEY (created_by) REFERENCES auth.users(id) |
| `maintenance_logs_vehicle_unit_id_fkey` | FOREIGN KEY (vehicle_unit_id) REFERENCES vehicle_units(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**CHECK constraints (values written out in full):**

- `maintenance_logs_maintenance_type_check`: `CHECK ((maintenance_type = ANY (ARRAY['scheduled_service'::text, 'repair'::text, 'inspection'::text, 'tire_replacement'::text, 'oil_change'::text, 'brake_service'::text, 'other'::text])))`

**Indexes:**

- `idx_maintenance_logs_service_date`: `CREATE INDEX idx_maintenance_logs_service_date ON public.maintenance_logs USING btree (service_date)`
- `idx_maintenance_logs_vehicle_unit_id`: `CREATE INDEX idx_maintenance_logs_vehicle_unit_id ON public.maintenance_logs USING btree (vehicle_unit_id)`
- `maintenance_logs_pkey`: `CREATE UNIQUE INDEX maintenance_logs_pkey ON public.maintenance_logs USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `membership_tiers`

**Purpose:** Loyalty tier definitions.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `name` | text | no | — |
| `display_name` | text | no | — |
| `min_points` | integer | no | `0` |
| `benefits` | jsonb | yes | `'[]'::jsonb` |
| `color` | text | yes | `'#CD7F32'::text` |
| `icon` | text | yes | `'medal'::text` |
| `sort_order` | integer | no | — |
| `is_active` | boolean | yes | `true` |
| `created_at` | timestamp with time zone | yes | `now()` |
| `updated_at` | timestamp with time zone | yes | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:** none

**Unique constraints:**

- `membership_tiers_name_key`: UNIQUE (name)

**Indexes:**

- `membership_tiers_name_key`: `CREATE UNIQUE INDEX membership_tiers_name_key ON public.membership_tiers USING btree (name)`
- `membership_tiers_pkey`: `CREATE UNIQUE INDEX membership_tiers_pkey ON public.membership_tiers USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `notification_logs`

**Purpose:** Idempotency + delivery log for every email/SMS attempt.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `channel` | text | no | — |
| `notification_type` | text | no | — |
| `booking_id` | uuid | yes | — |
| `user_id` | uuid | yes | — |
| `idempotency_key` | text | no | — |
| `status` | text | no | `'pending'::text` |
| `provider_id` | text | yes | — |
| `error_message` | text | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `sent_at` | timestamp with time zone | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `notification_logs_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Unique constraints:**

- `notification_logs_idempotency_key_key`: UNIQUE (idempotency_key)

**CHECK constraints (values written out in full):**

- `notification_logs_channel_check`: `CHECK ((channel = ANY (ARRAY['sms'::text, 'email'::text])))`
- `notification_logs_status_check`: `CHECK ((status = ANY (ARRAY['pending'::text, 'sent'::text, 'failed'::text])))`

**Indexes:**

- `idx_notification_logs_booking`: `CREATE INDEX idx_notification_logs_booking ON public.notification_logs USING btree (booking_id)`
- `idx_notification_logs_idempotency`: `CREATE INDEX idx_notification_logs_idempotency ON public.notification_logs USING btree (idempotency_key)`
- `notification_logs_idempotency_key_key`: `CREATE UNIQUE INDEX notification_logs_idempotency_key_key ON public.notification_logs USING btree (idempotency_key)`
- `notification_logs_pkey`: `CREATE UNIQUE INDEX notification_logs_pkey ON public.notification_logs USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `offer_redemptions`

**Purpose:** Points-offer redemptions per user/booking.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `offer_id` | uuid | no | — |
| `user_id` | uuid | no | — |
| `booking_id` | uuid | yes | — |
| `points_spent` | integer | no | — |
| `discount_value` | numeric(10,2) | no | — |
| `redeemed_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `offer_redemptions_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE SET NULL |
| `offer_redemptions_offer_id_fkey` | FOREIGN KEY (offer_id) REFERENCES points_offers(id) ON DELETE CASCADE |
| `offer_redemptions_user_id_fkey` | FOREIGN KEY (user_id) REFERENCES auth.users(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Unique constraints:**

- `offer_redemptions_offer_id_booking_id_key`: UNIQUE (offer_id, booking_id)

**Indexes:**

- `idx_offer_redemptions_offer`: `CREATE INDEX idx_offer_redemptions_offer ON public.offer_redemptions USING btree (offer_id)`
- `idx_offer_redemptions_user`: `CREATE INDEX idx_offer_redemptions_user ON public.offer_redemptions USING btree (user_id)`
- `offer_redemptions_offer_id_booking_id_key`: `CREATE UNIQUE INDEX offer_redemptions_offer_id_booking_id_key ON public.offer_redemptions USING btree (offer_id, booking_id)`
- `offer_redemptions_pkey`: `CREATE UNIQUE INDEX offer_redemptions_pkey ON public.offer_redemptions USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `payment_profiles`

**Purpose:** Tokenised Worldline card profiles (card-on-file).

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `user_id` | uuid | no | — |
| `worldline_customer_code` | text | no | — |
| `card_last_four` | text | yes | — |
| `card_type` | text | yes | — |
| `card_expiry_month` | integer | yes | — |
| `card_expiry_year` | integer | yes | — |
| `is_default` | boolean | yes | `false` |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:** none

**Unique constraints:**

- `payment_profiles_user_id_worldline_customer_code_key`: UNIQUE (user_id, worldline_customer_code)

**Indexes:**

- `payment_profiles_pkey`: `CREATE UNIQUE INDEX payment_profiles_pkey ON public.payment_profiles USING btree (id)`
- `payment_profiles_user_id_worldline_customer_code_key`: `CREATE UNIQUE INDEX payment_profiles_user_id_worldline_customer_code_key ON public.payment_profiles USING btree (user_id, worldline_customer_code)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `payments`

**Purpose:** Rental payment rows (online, terminal, manual). Source of truth for revenue.

**RLS enabled:** yes · **FORCE RLS:** yes

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `user_id` | uuid | no | — |
| `amount` | numeric(10,2) | no | — |
| `payment_type` | text | no | — |
| `payment_method` | text | yes | — |
| `transaction_id` | text | yes | — |
| `status` | text | no | `'pending'::text` |
| `created_at` | timestamp with time zone | no | `now()` |
| `location_id` | text | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `payments_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `idx_payments_location_id`: `CREATE INDEX idx_payments_location_id ON public.payments USING btree (location_id)`
- `idx_payments_unique_transaction_id`: `CREATE UNIQUE INDEX idx_payments_unique_transaction_id ON public.payments USING btree (transaction_id) WHERE (transaction_id IS NOT NULL)`
- `payments_pkey`: `CREATE UNIQUE INDEX payments_pkey ON public.payments USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `points_ledger`

**Purpose:** Loyalty points transactions with running balance.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `user_id` | uuid | no | — |
| `booking_id` | uuid | yes | — |
| `transaction_type` | points_transaction_type | no | — |
| `points` | integer | no | — |
| `money_value` | numeric(10,2) | yes | — |
| `balance_after` | integer | no | — |
| `notes` | text | yes | — |
| `created_by` | uuid | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `expires_at` | timestamp with time zone | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `points_ledger_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE SET NULL |
| `points_ledger_user_id_fkey` | FOREIGN KEY (user_id) REFERENCES auth.users(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `idx_points_ledger_booking_id`: `CREATE INDEX idx_points_ledger_booking_id ON public.points_ledger USING btree (booking_id)`
- `idx_points_ledger_created_at`: `CREATE INDEX idx_points_ledger_created_at ON public.points_ledger USING btree (created_at DESC)`
- `idx_points_ledger_expires_at`: `CREATE INDEX idx_points_ledger_expires_at ON public.points_ledger USING btree (expires_at) WHERE (expires_at IS NOT NULL)`
- `idx_points_ledger_user_id`: `CREATE INDEX idx_points_ledger_user_id ON public.points_ledger USING btree (user_id)`
- `points_ledger_pkey`: `CREATE UNIQUE INDEX points_ledger_pkey ON public.points_ledger USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `points_offers`

**Purpose:** Redeemable loyalty offers.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `name` | text | no | — |
| `description` | text | yes | — |
| `offer_type` | offer_type | no | — |
| `offer_value` | numeric(10,2) | no | — |
| `points_required` | integer | no | — |
| `is_active` | boolean | no | `true` |
| `valid_from` | timestamp with time zone | yes | — |
| `valid_until` | timestamp with time zone | yes | — |
| `min_rental_days` | integer | yes | — |
| `eligible_categories` | _uuid | yes | — |
| `eligible_locations` | _uuid | yes | — |
| `max_uses_total` | integer | yes | — |
| `max_uses_per_user` | integer | yes | `1` |
| `current_uses` | integer | yes | `0` |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |
| `created_by` | uuid | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:** none

**Indexes:**

- `idx_points_offers_active`: `CREATE INDEX idx_points_offers_active ON public.points_offers USING btree (is_active) WHERE (is_active = true)`
- `idx_points_offers_valid`: `CREATE INDEX idx_points_offers_valid ON public.points_offers USING btree (valid_from, valid_until)`
- `points_offers_pkey`: `CREATE UNIQUE INDEX points_offers_pkey ON public.points_offers USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `points_settings`

**Purpose:** Global loyalty configuration.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `setting_key` | text | no | — |
| `setting_value` | jsonb | no | — |
| `description` | text | yes | — |
| `updated_at` | timestamp with time zone | no | `now()` |
| `updated_by` | uuid | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:** none

**Unique constraints:**

- `points_settings_setting_key_key`: UNIQUE (setting_key)

**Indexes:**

- `points_settings_pkey`: `CREATE UNIQUE INDEX points_settings_pkey ON public.points_settings USING btree (id)`
- `points_settings_setting_key_key`: `CREATE UNIQUE INDEX points_settings_setting_key_key ON public.points_settings USING btree (setting_key)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `profiles`

**Purpose:** Auth-user profile: name, phone, licence, membership, points balance.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | — |
| `email` | text | yes | — |
| `full_name` | text | yes | — |
| `phone` | text | yes | — |
| `avatar_url` | text | yes | — |
| `role` | staff_role | yes | — |
| `is_verified` | boolean | yes | `false` |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |
| `driver_license_status` | text | yes | `'missing'::text` |
| `driver_license_front_url` | text | yes | — |
| `driver_license_back_url` | text | yes | — |
| `driver_license_expiry` | date | yes | — |
| `driver_license_uploaded_at` | timestamp with time zone | yes | — |
| `driver_license_reviewed_by` | uuid | yes | — |
| `driver_license_reviewed_at` | timestamp with time zone | yes | — |
| `member_id` | text | yes | — |
| `points_balance` | integer | no | `0` |
| `membership_tier` | membership_tier | yes | `'bronze'::membership_tier` |
| `membership_joined_at` | timestamp with time zone | yes | — |
| `membership_status` | membership_status | yes | `'active'::membership_status` |
| `driver_license_number` | text | yes | — |
| `address` | text | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:** none

**Unique constraints:**

- `profiles_member_id_key`: UNIQUE (member_id)

**CHECK constraints (values written out in full):**

- `profiles_driver_license_status_check`: `CHECK ((driver_license_status = ANY (ARRAY['missing'::text, 'on_file'::text, 'expired'::text])))`

**Indexes:**

- `idx_profiles_driver_license_status`: `CREATE INDEX idx_profiles_driver_license_status ON public.profiles USING btree (driver_license_status)`
- `idx_profiles_email_unique`: `CREATE UNIQUE INDEX idx_profiles_email_unique ON public.profiles USING btree (email) WHERE (email IS NOT NULL)`
- `idx_profiles_member_id`: `CREATE INDEX idx_profiles_member_id ON public.profiles USING btree (member_id)`
- `profiles_member_id_key`: `CREATE UNIQUE INDEX profiles_member_id_key ON public.profiles USING btree (member_id)`
- `profiles_pkey`: `CREATE UNIQUE INDEX profiles_pkey ON public.profiles USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `rate_limits`

**Purpose:** Atomic rate-limit counters used by edge functions via check_rate_limit().

**RLS enabled:** yes · **FORCE RLS:** yes

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `key` | text | no | — |
| `window_start` | timestamp with time zone | no | `now()` |
| `request_count` | integer | no | `1` |
| `window_seconds` | integer | no | `600` |
| `max_requests` | integer | no | `5` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:** none

**Unique constraints:**

- `rate_limits_key_key`: UNIQUE (key)

**Indexes:**

- `rate_limits_key_key`: `CREATE UNIQUE INDEX rate_limits_key_key ON public.rate_limits USING btree (key)`
- `rate_limits_pkey`: `CREATE UNIQUE INDEX rate_limits_pkey ON public.rate_limits USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `receipt_events`

**Purpose:** Lifecycle events for receipts.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `receipt_id` | uuid | no | — |
| `actor_user_id` | uuid | no | — |
| `action` | text | no | — |
| `meta_json` | jsonb | yes | `'{}'::jsonb` |
| `created_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `receipt_events_receipt_id_fkey` | FOREIGN KEY (receipt_id) REFERENCES receipts(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `receipt_events_pkey`: `CREATE UNIQUE INDEX receipt_events_pkey ON public.receipt_events USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `receipts`

**Purpose:** Generated customer receipts.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `receipt_number` | text | no | — |
| `status` | receipt_status | no | `'draft'::receipt_status` |
| `currency` | text | yes | `'USD'::text` |
| `line_items_json` | jsonb | no | `'[]'::jsonb` |
| `totals_json` | jsonb | no | `'{}'::jsonb` |
| `notes` | text | yes | — |
| `issued_at` | timestamp with time zone | yes | — |
| `created_by` | uuid | no | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `receipts_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Unique constraints:**

- `receipts_receipt_number_key`: UNIQUE (receipt_number)

**Indexes:**

- `receipts_pkey`: `CREATE UNIQUE INDEX receipts_pkey ON public.receipts USING btree (id)`
- `receipts_receipt_number_key`: `CREATE UNIQUE INDEX receipts_receipt_number_key ON public.receipts USING btree (receipt_number)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `rental_agreements`

**Purpose:** Generated rental/extension agreements and their signature state.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `agreement_content` | text | no | — |
| `terms_json` | jsonb | no | `'{}'::jsonb` |
| `customer_signature` | text | yes | — |
| `customer_signed_at` | timestamp with time zone | yes | — |
| `customer_ip_address` | text | yes | — |
| `staff_confirmed_by` | uuid | yes | — |
| `staff_confirmed_at` | timestamp with time zone | yes | — |
| `status` | text | no | `'pending'::text` |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |
| `signed_manually` | boolean | yes | `false` |
| `signed_manually_by` | uuid | yes | — |
| `signed_manually_at` | timestamp with time zone | yes | — |
| `signature_png_url` | text | yes | — |
| `signature_vector_json` | jsonb | yes | — |
| `signature_method` | text | yes | — |
| `signature_device_info` | jsonb | yes | — |
| `signature_workstation_id` | text | yes | — |
| `agreement_type` | text | no | `'initial'::text` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `rental_agreements_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**CHECK constraints (values written out in full):**

- `rental_agreements_status_check`: `CHECK ((status = ANY (ARRAY['pending'::text, 'signed'::text, 'confirmed'::text, 'voided'::text])))`

**Indexes:**

- `idx_rental_agreements_booking_id`: `CREATE INDEX idx_rental_agreements_booking_id ON public.rental_agreements USING btree (booking_id)`
- `rental_agreements_pkey`: `CREATE UNIQUE INDEX rental_agreements_pkey ON public.rental_agreements USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `reservation_holds`

**Purpose:** Short-lived checkout holds that reserve category capacity.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `user_id` | uuid | no | — |
| `vehicle_id` | uuid | yes | — |
| `start_at` | timestamp with time zone | no | — |
| `end_at` | timestamp with time zone | no | — |
| `expires_at` | timestamp with time zone | no | — |
| `status` | hold_status | no | `'active'::hold_status` |
| `created_at` | timestamp with time zone | no | `now()` |
| `category_id` | uuid | yes | — |
| `location_id` | uuid | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `reservation_holds_category_id_fkey` | FOREIGN KEY (category_id) REFERENCES vehicle_categories(id) ON DELETE CASCADE |
| `reservation_holds_location_id_fkey` | FOREIGN KEY (location_id) REFERENCES locations(id) ON DELETE SET NULL |
| `reservation_holds_vehicle_id_fkey` | FOREIGN KEY (vehicle_id) REFERENCES vehicles(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `idx_holds_category_location`: `CREATE INDEX idx_holds_category_location ON public.reservation_holds USING btree (category_id, location_id, status, expires_at)`
- `idx_holds_dates`: `CREATE INDEX idx_holds_dates ON public.reservation_holds USING btree (start_at, end_at)`
- `idx_holds_status`: `CREATE INDEX idx_holds_status ON public.reservation_holds USING btree (status)`
- `idx_holds_vehicle`: `CREATE INDEX idx_holds_vehicle ON public.reservation_holds USING btree (vehicle_id)`
- `reservation_holds_pkey`: `CREATE UNIQUE INDEX reservation_holds_pkey ON public.reservation_holds USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `staff_assignments`

**Purpose:** Which staff user works at which location, with role and active flag.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `user_id` | uuid | no | — |
| `location_id` | uuid | yes | — |
| `display_name` | text | yes | — |
| `employee_code` | text | yes | — |
| `is_active` | boolean | no | `true` |
| `created_by` | uuid | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |
| `sms_alerts_enabled` | boolean | no | `true` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `staff_assignments_location_id_fkey` | FOREIGN KEY (location_id) REFERENCES locations(id) ON DELETE SET NULL |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Unique constraints:**

- `staff_assignments_user_id_key`: UNIQUE (user_id)

**Indexes:**

- `idx_staff_assignments_location`: `CREATE INDEX idx_staff_assignments_location ON public.staff_assignments USING btree (location_id) WHERE (is_active = true)`
- `staff_assignments_pkey`: `CREATE UNIQUE INDEX staff_assignments_pkey ON public.staff_assignments USING btree (id)`
- `staff_assignments_user_id_key`: `CREATE UNIQUE INDEX staff_assignments_user_id_key ON public.staff_assignments USING btree (user_id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `support_macros`

**Purpose:** Canned support replies.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `name` | text | no | — |
| `category` | text | no | `'general'::text` |
| `content` | text | no | — |
| `variables` | jsonb | yes | `'[]'::jsonb` |
| `is_active` | boolean | no | `true` |
| `usage_count` | integer | yes | `0` |
| `created_by` | uuid | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:** none

**Indexes:**

- `support_macros_pkey`: `CREATE UNIQUE INDEX support_macros_pkey ON public.support_macros USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `support_tickets_v2`

**Purpose:** Active support ticket table (TKT- numbering, category, priority, SLA fields).

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `ticket_id` | text | no | — |
| `status` | text | no | `'new'::text` |
| `category` | text | no | `'general'::text` |
| `priority` | text | no | `'medium'::text` |
| `is_urgent` | boolean | no | `false` |
| `assigned_to` | uuid | yes | — |
| `customer_id` | uuid | yes | — |
| `booking_id` | uuid | yes | — |
| `incident_id` | uuid | yes | — |
| `guest_email` | text | yes | — |
| `guest_phone` | text | yes | — |
| `guest_name` | text | yes | — |
| `subject` | text | no | — |
| `description` | text | no | — |
| `resolution_note` | text | yes | — |
| `escalation_note` | text | yes | — |
| `escalated_at` | timestamp with time zone | yes | — |
| `escalated_by` | uuid | yes | — |
| `first_response_at` | timestamp with time zone | yes | — |
| `closed_at` | timestamp with time zone | yes | — |
| `closed_by` | uuid | yes | — |
| `created_by` | uuid | no | — |
| `created_by_type` | text | no | `'customer'::text` |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |
| `damage_id` | uuid | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `support_tickets_v2_damage_id_fkey` | FOREIGN KEY (damage_id) REFERENCES damage_reports(id) ON DELETE SET NULL |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Unique constraints:**

- `support_tickets_v2_ticket_id_key`: UNIQUE (ticket_id)

**CHECK constraints (values written out in full):**

- `support_tickets_v2_category_check`: `CHECK ((category = ANY (ARRAY['billing'::text, 'booking'::text, 'ops'::text, 'damage'::text, 'website_bug'::text, 'general'::text, 'incident'::text])))`
- `support_tickets_v2_created_by_type_check`: `CHECK ((created_by_type = ANY (ARRAY['customer'::text, 'admin'::text, 'support'::text, 'system'::text, 'staff'::text])))`
- `support_tickets_v2_priority_check`: `CHECK ((priority = ANY (ARRAY['low'::text, 'medium'::text, 'high'::text])))`
- `support_tickets_v2_status_check`: `CHECK ((status = ANY (ARRAY['new'::text, 'in_progress'::text, 'waiting_customer'::text, 'escalated'::text, 'closed'::text])))`

**Indexes:**

- `idx_support_tickets_v2_damage_id`: `CREATE INDEX idx_support_tickets_v2_damage_id ON public.support_tickets_v2 USING btree (damage_id)`
- `idx_tickets_v2_assigned`: `CREATE INDEX idx_tickets_v2_assigned ON public.support_tickets_v2 USING btree (assigned_to)`
- `idx_tickets_v2_booking`: `CREATE INDEX idx_tickets_v2_booking ON public.support_tickets_v2 USING btree (booking_id)`
- `idx_tickets_v2_category`: `CREATE INDEX idx_tickets_v2_category ON public.support_tickets_v2 USING btree (category)`
- `idx_tickets_v2_created`: `CREATE INDEX idx_tickets_v2_created ON public.support_tickets_v2 USING btree (created_at DESC)`
- `idx_tickets_v2_customer`: `CREATE INDEX idx_tickets_v2_customer ON public.support_tickets_v2 USING btree (customer_id)`
- `idx_tickets_v2_incident`: `CREATE INDEX idx_tickets_v2_incident ON public.support_tickets_v2 USING btree (incident_id)`
- `idx_tickets_v2_priority`: `CREATE INDEX idx_tickets_v2_priority ON public.support_tickets_v2 USING btree (priority)`
- `idx_tickets_v2_status`: `CREATE INDEX idx_tickets_v2_status ON public.support_tickets_v2 USING btree (status)`
- `idx_tickets_v2_ticket_id`: `CREATE INDEX idx_tickets_v2_ticket_id ON public.support_tickets_v2 USING btree (ticket_id)`
- `idx_tickets_v2_urgent`: `CREATE INDEX idx_tickets_v2_urgent ON public.support_tickets_v2 USING btree (is_urgent) WHERE (is_urgent = true)`
- `support_tickets_v2_pkey`: `CREATE UNIQUE INDEX support_tickets_v2_pkey ON public.support_tickets_v2 USING btree (id)`
- `support_tickets_v2_ticket_id_key`: `CREATE UNIQUE INDEX support_tickets_v2_ticket_id_key ON public.support_tickets_v2 USING btree (ticket_id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `system_settings`

**Purpose:** Key/value application settings (e.g. card-view password).

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `key` | text | no | — |
| `value` | text | no | — |
| `description` | text | yes | — |
| `updated_at` | timestamp with time zone | no | `now()` |
| `updated_by` | uuid | yes | — |

**Primary key:** PRIMARY KEY (key)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `system_settings_updated_by_fkey` | FOREIGN KEY (updated_by) REFERENCES auth.users(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `system_settings_pkey`: `CREATE UNIQUE INDEX system_settings_pkey ON public.system_settings USING btree (key)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `ticket_attachments`

**Purpose:** Files attached to support tickets.

**Database comment:** Files/photos attached to tickets as proof or evidence

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `ticket_id` | uuid | no | — |
| `user_id` | uuid | no | — |
| `file_url` | text | no | — |
| `file_name` | text | no | — |
| `file_type` | text | yes | — |
| `is_internal` | boolean | no | `false` |
| `created_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `ticket_attachments_ticket_id_fkey` | FOREIGN KEY (ticket_id) REFERENCES tickets(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `idx_ticket_attachments_ticket_id`: `CREATE INDEX idx_ticket_attachments_ticket_id ON public.ticket_attachments USING btree (ticket_id)`
- `ticket_attachments_pkey`: `CREATE UNIQUE INDEX ticket_attachments_pkey ON public.ticket_attachments USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `ticket_audit_log`

**Purpose:** Audit trail of support ticket field changes.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `ticket_id` | uuid | no | — |
| `action` | text | no | — |
| `performed_by` | uuid | no | — |
| `old_value` | jsonb | yes | — |
| `new_value` | jsonb | yes | — |
| `note` | text | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `ticket_audit_log_ticket_id_fkey` | FOREIGN KEY (ticket_id) REFERENCES support_tickets_v2(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `idx_ticket_audit_action`: `CREATE INDEX idx_ticket_audit_action ON public.ticket_audit_log USING btree (action)`
- `idx_ticket_audit_created`: `CREATE INDEX idx_ticket_audit_created ON public.ticket_audit_log USING btree (created_at DESC)`
- `idx_ticket_audit_ticket`: `CREATE INDEX idx_ticket_audit_ticket ON public.ticket_audit_log USING btree (ticket_id)`
- `ticket_audit_log_pkey`: `CREATE UNIQUE INDEX ticket_audit_log_pkey ON public.ticket_audit_log USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `ticket_messages`

**Purpose:** Messages on the legacy tickets table.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `ticket_id` | uuid | no | — |
| `sender_id` | uuid | no | — |
| `message` | text | no | — |
| `is_staff` | boolean | yes | `false` |
| `created_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `ticket_messages_ticket_id_fkey` | FOREIGN KEY (ticket_id) REFERENCES tickets(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `ticket_messages_pkey`: `CREATE UNIQUE INDEX ticket_messages_pkey ON public.ticket_messages USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `ticket_messages_v2`

**Purpose:** Messages on support_tickets_v2, staff-internal or customer-visible.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `ticket_id` | uuid | no | — |
| `message` | text | no | — |
| `message_type` | text | no | `'customer_visible'::text` |
| `sender_id` | uuid | no | — |
| `sender_type` | text | no | `'customer'::text` |
| `macro_id` | uuid | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `ticket_messages_v2_macro_id_fkey` | FOREIGN KEY (macro_id) REFERENCES support_macros(id) |
| `ticket_messages_v2_ticket_id_fkey` | FOREIGN KEY (ticket_id) REFERENCES support_tickets_v2(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**CHECK constraints (values written out in full):**

- `ticket_messages_v2_message_type_check`: `CHECK ((message_type = ANY (ARRAY['customer_visible'::text, 'internal_note'::text])))`
- `ticket_messages_v2_sender_type_check`: `CHECK ((sender_type = ANY (ARRAY['customer'::text, 'staff'::text, 'system'::text])))`

**Indexes:**

- `idx_ticket_messages_v2_ticket`: `CREATE INDEX idx_ticket_messages_v2_ticket ON public.ticket_messages_v2 USING btree (ticket_id)`
- `idx_ticket_messages_v2_type`: `CREATE INDEX idx_ticket_messages_v2_type ON public.ticket_messages_v2 USING btree (message_type)`
- `ticket_messages_v2_pkey`: `CREATE UNIQUE INDEX ticket_messages_v2_pkey ON public.ticket_messages_v2 USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `ticket_timeline`

**Purpose:** Ticket status/action timeline entries.

**Database comment:** Audit trail for ticket status changes and actions

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `ticket_id` | uuid | no | — |
| `user_id` | uuid | no | — |
| `action` | text | no | — |
| `old_status` | text | yes | — |
| `new_status` | text | yes | — |
| `note` | text | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `ticket_timeline_ticket_id_fkey` | FOREIGN KEY (ticket_id) REFERENCES tickets(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `idx_ticket_timeline_ticket_id`: `CREATE INDEX idx_ticket_timeline_ticket_id ON public.ticket_timeline USING btree (ticket_id)`
- `ticket_timeline_pkey`: `CREATE UNIQUE INDEX ticket_timeline_pkey ON public.ticket_timeline USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `tickets`

**Purpose:** Legacy support ticket table, superseded by support_tickets_v2.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | yes | — |
| `user_id` | uuid | no | — |
| `subject` | text | no | — |
| `status` | ticket_status | no | `'open'::ticket_status` |
| `priority` | text | yes | `'normal'::text` |
| `assigned_to` | uuid | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |
| `resolution_summary` | text | yes | — |
| `resolved_at` | timestamp with time zone | yes | — |
| `resolved_by` | uuid | yes | — |
| `escalated_at` | timestamp with time zone | yes | — |
| `escalated_from` | text | yes | — |
| `escalation_count` | integer | yes | `0` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `tickets_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `idx_tickets_status`: `CREATE INDEX idx_tickets_status ON public.tickets USING btree (status)`
- `idx_tickets_user`: `CREATE INDEX idx_tickets_user ON public.tickets USING btree (user_id)`
- `tickets_pkey`: `CREATE UNIQUE INDEX tickets_pkey ON public.tickets USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `user_roles`

**Purpose:** Role grants per auth user; read by has_role()/is_* security-definer helpers.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `user_id` | uuid | no | — |
| `role` | app_role | no | — |
| `created_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `user_roles_user_id_fkey` | FOREIGN KEY (user_id) REFERENCES auth.users(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Unique constraints:**

- `user_roles_user_id_role_key`: UNIQUE (user_id, role)

**Indexes:**

- `user_roles_pkey`: `CREATE UNIQUE INDEX user_roles_pkey ON public.user_roles USING btree (id)`
- `user_roles_user_id_role_key`: `CREATE UNIQUE INDEX user_roles_user_id_role_key ON public.user_roles USING btree (user_id, role)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `vehicle_categories`

**Purpose:** Bookable vehicle classes with the published daily rate.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `name` | text | no | — |
| `description` | text | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |
| `created_by` | uuid | yes | — |
| `image_url` | text | yes | — |
| `daily_rate` | numeric | no | `50` |
| `is_active` | boolean | no | `true` |
| `seats` | integer | yes | `5` |
| `fuel_type` | text | yes | `'Gas'::text` |
| `transmission` | text | yes | `'Automatic'::text` |
| `sort_order` | integer | yes | `0` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `vehicle_categories_created_by_fkey` | FOREIGN KEY (created_by) REFERENCES auth.users(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Unique constraints:**

- `vehicle_categories_name_key`: UNIQUE (name)

**Indexes:**

- `vehicle_categories_name_key`: `CREATE UNIQUE INDEX vehicle_categories_name_key ON public.vehicle_categories USING btree (name)`
- `vehicle_categories_pkey`: `CREATE UNIQUE INDEX vehicle_categories_pkey ON public.vehicle_categories USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `vehicle_expenses`

**Purpose:** Per-vehicle expense records.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `vehicle_unit_id` | uuid | no | — |
| `expense_type` | text | no | — |
| `amount` | numeric | no | — |
| `description` | text | yes | — |
| `expense_date` | date | no | `CURRENT_DATE` |
| `vendor` | text | yes | — |
| `receipt_url` | text | yes | — |
| `mileage_at_expense` | integer | yes | — |
| `created_by` | uuid | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `vehicle_expenses_vehicle_unit_id_fkey` | FOREIGN KEY (vehicle_unit_id) REFERENCES vehicle_units(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `idx_vehicle_expenses_date`: `CREATE INDEX idx_vehicle_expenses_date ON public.vehicle_expenses USING btree (expense_date)`
- `idx_vehicle_expenses_expense_date`: `CREATE INDEX idx_vehicle_expenses_expense_date ON public.vehicle_expenses USING btree (expense_date)`
- `idx_vehicle_expenses_type`: `CREATE INDEX idx_vehicle_expenses_type ON public.vehicle_expenses USING btree (expense_type)`
- `idx_vehicle_expenses_unit_id`: `CREATE INDEX idx_vehicle_expenses_unit_id ON public.vehicle_expenses USING btree (vehicle_unit_id)`
- `idx_vehicle_expenses_vehicle_unit_id`: `CREATE INDEX idx_vehicle_expenses_vehicle_unit_id ON public.vehicle_expenses USING btree (vehicle_unit_id)`
- `vehicle_expenses_pkey`: `CREATE UNIQUE INDEX vehicle_expenses_pkey ON public.vehicle_expenses USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `vehicle_swap_history`

**Purpose:** Record of swapping the assigned unit on a booking.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `old_unit_id` | uuid | yes | — |
| `new_unit_id` | uuid | yes | — |
| `old_agreement_id` | uuid | yes | — |
| `new_agreement_id` | uuid | yes | — |
| `swap_effective_at` | timestamp with time zone | no | `now()` |
| `old_end_mileage` | integer | yes | — |
| `new_start_mileage` | integer | no | — |
| `reason` | text | yes | — |
| `notes` | text | yes | — |
| `old_vin` | text | yes | — |
| `old_license_plate` | text | yes | — |
| `new_vin` | text | yes | — |
| `new_license_plate` | text | yes | — |
| `changed_by` | uuid | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `vehicle_swap_history_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE CASCADE |
| `vehicle_swap_history_new_agreement_id_fkey` | FOREIGN KEY (new_agreement_id) REFERENCES rental_agreements(id) ON DELETE SET NULL |
| `vehicle_swap_history_new_unit_id_fkey` | FOREIGN KEY (new_unit_id) REFERENCES vehicle_units(id) ON DELETE SET NULL |
| `vehicle_swap_history_old_agreement_id_fkey` | FOREIGN KEY (old_agreement_id) REFERENCES rental_agreements(id) ON DELETE SET NULL |
| `vehicle_swap_history_old_unit_id_fkey` | FOREIGN KEY (old_unit_id) REFERENCES vehicle_units(id) ON DELETE SET NULL |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `idx_vehicle_swap_history_booking`: `CREATE INDEX idx_vehicle_swap_history_booking ON public.vehicle_swap_history USING btree (booking_id, created_at DESC)`
- `vehicle_swap_history_pkey`: `CREATE UNIQUE INDEX vehicle_swap_history_pkey ON public.vehicle_swap_history USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `vehicle_units`

**Purpose:** Physical vehicles (VIN/plate) with status and home location.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `vehicle_id` | uuid | no | — |
| `vin` | text | no | — |
| `acquisition_cost` | numeric | no | `0` |
| `acquisition_date` | date | yes | — |
| `license_plate` | text | yes | — |
| `color` | text | yes | — |
| `mileage_at_acquisition` | integer | yes | — |
| `current_mileage` | integer | yes | — |
| `notes` | text | yes | — |
| `status` | text | no | `'active'::text` |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |
| `vendor_name` | text | yes | — |
| `vendor_contact` | text | yes | — |
| `vendor_notes` | text | yes | — |
| `depreciation_method` | text | yes | `'straight_line'::text` |
| `annual_depreciation_amount` | numeric | yes | `0` |
| `expected_disposal_date` | date | yes | — |
| `actual_disposal_date` | date | yes | — |
| `disposal_value` | numeric | yes | `0` |
| `category_id` | uuid | yes | — |
| `location_id` | uuid | yes | — |
| `tank_capacity_liters` | numeric(5,1) | yes | `60` |
| `is_temporary` | boolean | no | `false` |
| `temp_source` | text | yes | — |
| `temp_start_date` | date | yes | — |
| `temp_end_date` | date | yes | — |
| `temp_daily_cost` | numeric | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `vehicle_units_category_id_fkey` | FOREIGN KEY (category_id) REFERENCES vehicle_categories(id) ON DELETE SET NULL |
| `vehicle_units_location_id_fkey` | FOREIGN KEY (location_id) REFERENCES locations(id) |
| `vehicle_units_vehicle_id_fkey` | FOREIGN KEY (vehicle_id) REFERENCES vehicles(id) ON DELETE CASCADE |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Unique constraints:**

- `vehicle_units_vin_key`: UNIQUE (vin)

**Indexes:**

- `idx_vehicle_units_category_id`: `CREATE INDEX idx_vehicle_units_category_id ON public.vehicle_units USING btree (category_id)`
- `idx_vehicle_units_is_temporary`: `CREATE INDEX idx_vehicle_units_is_temporary ON public.vehicle_units USING btree (is_temporary) WHERE (is_temporary = true)`
- `idx_vehicle_units_location_id`: `CREATE INDEX idx_vehicle_units_location_id ON public.vehicle_units USING btree (location_id)`
- `idx_vehicle_units_status`: `CREATE INDEX idx_vehicle_units_status ON public.vehicle_units USING btree (status)`
- `idx_vehicle_units_vehicle_id`: `CREATE INDEX idx_vehicle_units_vehicle_id ON public.vehicle_units USING btree (vehicle_id)`
- `idx_vehicle_units_vin`: `CREATE INDEX idx_vehicle_units_vin ON public.vehicle_units USING btree (vin)`
- `vehicle_units_pkey`: `CREATE UNIQUE INDEX vehicle_units_pkey ON public.vehicle_units USING btree (id)`
- `vehicle_units_vin_key`: `CREATE UNIQUE INDEX vehicle_units_vin_key ON public.vehicle_units USING btree (vin)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `vehicles`

**Purpose:** Legacy per-vehicle listing table, largely superseded by vehicle_categories + vehicle_units.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `location_id` | uuid | yes | — |
| `make` | text | no | — |
| `model` | text | no | — |
| `year` | integer | no | — |
| `category` | text | no | — |
| `daily_rate` | numeric(10,2) | no | — |
| `image_url` | text | yes | — |
| `images_json` | jsonb | yes | `'[]'::jsonb` |
| `features_json` | jsonb | yes | `'[]'::jsonb` |
| `specs_json` | jsonb | yes | `'{}'::jsonb` |
| `seats` | integer | yes | `5` |
| `fuel_type` | text | yes | `'Petrol'::text` |
| `transmission` | text | yes | `'Automatic'::text` |
| `cleaning_buffer_hours` | integer | yes | `2` |
| `is_available` | boolean | yes | `true` |
| `is_featured` | boolean | yes | `false` |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |
| `status` | text | yes | `'available'::text` |
| `maintenance_reason` | text | yes | — |
| `maintenance_until` | date | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `vehicles_location_id_fkey` | FOREIGN KEY (location_id) REFERENCES locations(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `idx_vehicles_available`: `CREATE INDEX idx_vehicles_available ON public.vehicles USING btree (is_available)`
- `idx_vehicles_category`: `CREATE INDEX idx_vehicles_category ON public.vehicles USING btree (category)`
- `idx_vehicles_location`: `CREATE INDEX idx_vehicles_location ON public.vehicles USING btree (location_id)`
- `vehicles_pkey`: `CREATE UNIQUE INDEX vehicles_pkey ON public.vehicles USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `vendors`

**Purpose:** Suppliers used for maintenance/repairs.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `name` | text | no | — |
| `vendor_type` | text | no | `'general'::text` |
| `contact_name` | text | yes | — |
| `contact_email` | text | yes | — |
| `contact_phone` | text | yes | — |
| `address` | text | yes | — |
| `city` | text | yes | — |
| `notes` | text | yes | — |
| `rating` | numeric(2,1) | yes | — |
| `is_active` | boolean | no | `true` |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |
| `created_by` | uuid | yes | — |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:** none

**CHECK constraints (values written out in full):**

- `vendors_rating_check`: `CHECK (((rating >= (0)::numeric) AND (rating <= (5)::numeric)))`

**Indexes:**

- `vendors_pkey`: `CREATE UNIQUE INDEX vendors_pkey ON public.vendors USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `verification_requests`

**Purpose:** Customer identity/licence document submissions awaiting staff review.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `user_id` | uuid | no | — |
| `booking_id` | uuid | yes | — |
| `document_type` | text | no | — |
| `document_url` | text | no | — |
| `status` | verification_status | no | `'pending'::verification_status` |
| `reviewer_notes` | text | yes | — |
| `reviewed_at` | timestamp with time zone | yes | — |
| `reviewed_by` | uuid | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `verification_requests_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Indexes:**

- `verification_requests_pkey`: `CREATE UNIQUE INDEX verification_requests_pkey ON public.verification_requests USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `walkaround_inspections`

**Purpose:** Structured walkaround inspection results.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `booking_id` | uuid | no | — |
| `exterior_notes` | text | yes | — |
| `scratches_dents` | jsonb | yes | `'[]'::jsonb` |
| `interior_notes` | text | yes | — |
| `interior_condition` | text | yes | — |
| `odometer_reading` | integer | yes | — |
| `fuel_level` | integer | yes | — |
| `conducted_by` | uuid | no | — |
| `conducted_at` | timestamp with time zone | no | `now()` |
| `customer_acknowledged` | boolean | no | `false` |
| `customer_acknowledged_at` | timestamp with time zone | yes | — |
| `customer_signature` | text | yes | — |
| `inspection_complete` | boolean | no | `false` |
| `completed_at` | timestamp with time zone | yes | — |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `walkaround_inspections_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**CHECK constraints (values written out in full):**

- `walkaround_inspections_fuel_level_check`: `CHECK (((fuel_level >= 0) AND (fuel_level <= 100)))`
- `walkaround_inspections_interior_condition_check`: `CHECK ((interior_condition = ANY (ARRAY['excellent'::text, 'good'::text, 'acceptable'::text, 'needs_attention'::text])))`

**Indexes:**

- `idx_walkaround_inspections_booking_id`: `CREATE INDEX idx_walkaround_inspections_booking_id ON public.walkaround_inspections USING btree (booking_id)`
- `walkaround_inspections_pkey`: `CREATE UNIQUE INDEX walkaround_inspections_pkey ON public.walkaround_inspections USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

### `webhook_events`

**Purpose:** Inbound payment webhook events, stored for idempotency.

**RLS enabled:** yes · **FORCE RLS:** no

| Column | Type | Nullable | Default |
| --- | --- | --- | --- |
| `id` | uuid | no | `gen_random_uuid()` |
| `event_id` | text | no | — |
| `event_type` | text | no | — |
| `provider` | text | no | `'worldline'::text` |
| `booking_id` | uuid | yes | — |
| `payload_hash` | text | yes | — |
| `result` | jsonb | yes | — |
| `processed_at` | timestamp with time zone | yes | `now()` |
| `created_at` | timestamp with time zone | no | `now()` |

**Primary key:** PRIMARY KEY (id)

**Foreign keys:**

| Constraint | Definition (includes ON DELETE where set) |
| --- | --- |
| `webhook_events_booking_id_fkey` | FOREIGN KEY (booking_id) REFERENCES bookings(id) |

Any FK above without an explicit `ON DELETE` clause uses PostgreSQL's
default `ON DELETE NO ACTION`.

**Unique constraints:**

- `webhook_events_event_id_key`: UNIQUE (event_id)

**Indexes:**

- `webhook_events_event_id_key`: `CREATE UNIQUE INDEX webhook_events_event_id_key ON public.webhook_events USING btree (event_id)`
- `webhook_events_pkey`: `CREATE UNIQUE INDEX webhook_events_pkey ON public.webhook_events USING btree (id)`

**Data API grants:** none to `anon`/`authenticated`/`service_role` (table unreachable through the REST API).

---

## 2. Relationship map (text ERD)

Every foreign key in the schema. `many → one` reads: many rows of the left
table reference one row of the right table.

| Child (many) | Column(s) | Parent (one) | ON DELETE |
| --- | --- | --- | --- |
| `abandoned_carts` | `location_id` | `locations` (`id`) | NO ACTION (default) |
| `abandoned_carts` | `vehicle_id` | `vehicles` (`id`) | NO ACTION (default) |
| `admin_alerts` | `booking_id` | `bookings` (`id`) | NO ACTION (default) |
| `admin_alerts` | `ticket_id` | `support_tickets_v2` (`id`) | SET NULL |
| `admin_alerts` | `vehicle_id` | `vehicles` (`id`) | NO ACTION (default) |
| `bank_transfer_otps` | `booking_id` | `bookings` (`id`) | CASCADE |
| `booking_access_tokens` | `booking_id` | `bookings` (`id`) | CASCADE |
| `booking_add_ons` | `add_on_id` | `add_ons` (`id`) | NO ACTION (default) |
| `booking_add_ons` | `booking_id` | `bookings` (`id`) | CASCADE |
| `booking_additional_drivers` | `booking_id` | `bookings` (`id`) | CASCADE |
| `booking_documents` | `booking_id` | `bookings` (`id`) | CASCADE |
| `booking_documents` | `uploaded_at_location_id` | `locations` (`id`) | NO ACTION (default) |
| `booking_extensions` | `agreement_id` | `rental_agreements` (`id`) | SET NULL |
| `booking_extensions` | `booking_id` | `bookings` (`id`) | CASCADE |
| `booking_otps` | `booking_id` | `bookings` (`id`) | CASCADE |
| `bookings` | `account_closed_by` | `auth.users` (`id`) | NO ACTION (default) |
| `bookings` | `assigned_driver_id` | `auth.users` (`id`) | NO ACTION (default) |
| `bookings` | `assigned_unit_id` | `vehicle_units` (`id`) | NO ACTION (default) |
| `bookings` | `created_by` | `auth.users` (`id`) | NO ACTION (default) |
| `bookings` | `customer_id` | `customers` (`id`) | NO ACTION (default) |
| `bookings` | `internal_unit_category_id` | `vehicle_categories` (`id`) | NO ACTION (default) |
| `bookings` | `location_id` | `locations` (`id`) | NO ACTION (default) |
| `bookings` | `return_location_id` | `locations` (`id`) | NO ACTION (default) |
| `checkin_records` | `booking_id` | `bookings` (`id`) | CASCADE |
| `competitor_pricing` | `vehicle_id` | `vehicles` (`id`) | CASCADE |
| `condition_photos` | `booking_id` | `bookings` (`id`) | CASCADE |
| `damage_reports` | `booking_id` | `bookings` (`id`) | NO ACTION (default) |
| `damage_reports` | `vehicle_id` | `vehicle_categories` (`id`) | NO ACTION (default) |
| `damage_reports` | `vehicle_unit_id` | `vehicle_units` (`id`) | NO ACTION (default) |
| `delivery_status_log` | `booking_id` | `bookings` (`id`) | CASCADE |
| `delivery_status_log` | `created_by` | `auth.users` (`id`) | NO ACTION (default) |
| `delivery_statuses` | `booking_id` | `bookings` (`id`) | CASCADE |
| `delivery_tasks` | `assigned_driver_id` | `auth.users` (`id`) | NO ACTION (default) |
| `delivery_tasks` | `booking_id` | `bookings` (`id`) | CASCADE |
| `deposit_jobs` | `booking_id` | `bookings` (`id`) | NO ACTION (default) |
| `deposit_ledger` | `booking_id` | `bookings` (`id`) | CASCADE |
| `deposit_ledger` | `payment_id` | `payments` (`id`) | NO ACTION (default) |
| `final_invoices` | `booking_id` | `bookings` (`id`) | NO ACTION (default) |
| `final_invoices` | `created_by` | `auth.users` (`id`) | NO ACTION (default) |
| `fleet_cost_cache` | `category_id` | `vehicle_categories` (`id`) | CASCADE |
| `fleet_cost_cache` | `vehicle_unit_id` | `vehicle_units` (`id`) | CASCADE |
| `incident_cases` | `booking_id` | `bookings` (`id`) | SET NULL |
| `incident_cases` | `customer_id` | `auth.users` (`id`) | SET NULL |
| `incident_cases` | `vehicle_id` | `vehicle_categories` (`id`) | CASCADE |
| `incident_cases` | `vehicle_unit_id` | `vehicle_units` (`id`) | SET NULL |
| `incident_photos` | `incident_id` | `incident_cases` (`id`) | CASCADE |
| `incident_repairs` | `incident_id` | `incident_cases` (`id`) | CASCADE |
| `inspection_metrics` | `booking_id` | `bookings` (`id`) | CASCADE |
| `maintenance_logs` | `created_by` | `auth.users` (`id`) | NO ACTION (default) |
| `maintenance_logs` | `vehicle_unit_id` | `vehicle_units` (`id`) | CASCADE |
| `notification_logs` | `booking_id` | `bookings` (`id`) | NO ACTION (default) |
| `offer_redemptions` | `booking_id` | `bookings` (`id`) | SET NULL |
| `offer_redemptions` | `offer_id` | `points_offers` (`id`) | CASCADE |
| `offer_redemptions` | `user_id` | `auth.users` (`id`) | CASCADE |
| `payments` | `booking_id` | `bookings` (`id`) | NO ACTION (default) |
| `points_ledger` | `booking_id` | `bookings` (`id`) | SET NULL |
| `points_ledger` | `user_id` | `auth.users` (`id`) | CASCADE |
| `receipt_events` | `receipt_id` | `receipts` (`id`) | CASCADE |
| `receipts` | `booking_id` | `bookings` (`id`) | NO ACTION (default) |
| `rental_agreements` | `booking_id` | `bookings` (`id`) | NO ACTION (default) |
| `reservation_holds` | `category_id` | `vehicle_categories` (`id`) | CASCADE |
| `reservation_holds` | `location_id` | `locations` (`id`) | SET NULL |
| `reservation_holds` | `vehicle_id` | `vehicles` (`id`) | NO ACTION (default) |
| `staff_assignments` | `location_id` | `locations` (`id`) | SET NULL |
| `support_tickets_v2` | `damage_id` | `damage_reports` (`id`) | SET NULL |
| `system_settings` | `updated_by` | `auth.users` (`id`) | NO ACTION (default) |
| `ticket_attachments` | `ticket_id` | `tickets` (`id`) | CASCADE |
| `ticket_audit_log` | `ticket_id` | `support_tickets_v2` (`id`) | CASCADE |
| `ticket_messages` | `ticket_id` | `tickets` (`id`) | CASCADE |
| `ticket_messages_v2` | `macro_id` | `support_macros` (`id`) | NO ACTION (default) |
| `ticket_messages_v2` | `ticket_id` | `support_tickets_v2` (`id`) | CASCADE |
| `ticket_timeline` | `ticket_id` | `tickets` (`id`) | CASCADE |
| `tickets` | `booking_id` | `bookings` (`id`) | NO ACTION (default) |
| `user_roles` | `user_id` | `auth.users` (`id`) | CASCADE |
| `vehicle_categories` | `created_by` | `auth.users` (`id`) | NO ACTION (default) |
| `vehicle_expenses` | `vehicle_unit_id` | `vehicle_units` (`id`) | CASCADE |
| `vehicle_swap_history` | `booking_id` | `bookings` (`id`) | CASCADE |
| `vehicle_swap_history` | `new_agreement_id` | `rental_agreements` (`id`) | SET NULL |
| `vehicle_swap_history` | `new_unit_id` | `vehicle_units` (`id`) | SET NULL |
| `vehicle_swap_history` | `old_agreement_id` | `rental_agreements` (`id`) | SET NULL |
| `vehicle_swap_history` | `old_unit_id` | `vehicle_units` (`id`) | SET NULL |
| `vehicle_units` | `category_id` | `vehicle_categories` (`id`) | SET NULL |
| `vehicle_units` | `location_id` | `locations` (`id`) | NO ACTION (default) |
| `vehicle_units` | `vehicle_id` | `vehicles` (`id`) | CASCADE |
| `vehicles` | `location_id` | `locations` (`id`) | NO ACTION (default) |
| `verification_requests` | `booking_id` | `bookings` (`id`) | NO ACTION (default) |
| `walkaround_inspections` | `booking_id` | `bookings` (`id`) | NO ACTION (default) |
| `webhook_events` | `booking_id` | `bookings` (`id`) | NO ACTION (default) |

---

## 3. Enums and status values

### 3.1 Postgres enum types

| Type | Values |
| --- | --- |
| `alert_status` | pending, acknowledged, resolved |
| `alert_type` | verification_pending, payment_pending, cleaning_required, damage_reported, late_return, hold_expiring, return_due_soon, overdue, customer_issue, emergency |
| `app_role` | admin, staff, cleaner, finance, support, driver, super_admin, manager |
| `booking_status` | draft, pending, confirmed, active, completed, cancelled |
| `damage_severity` | minor, moderate, severe |
| `damage_status` | reported, reviewing, approved, repaired, closed |
| `hold_status` | active, expired, converted |
| `membership_status` | active, suspended, inactive |
| `membership_tier` | bronze, silver, gold, platinum |
| `offer_type` | percent_off, dollar_off, free_addon, free_upgrade |
| `points_transaction_type` | earn, redeem, adjust, expire, reverse |
| `receipt_status` | draft, issued, voided |
| `staff_role` | admin, staff, cleaner, finance |
| `ticket_status` | open, in_progress, resolved, closed, assigned, waiting_customer |
| `vehicle_status` | available, booked, active_rental, maintenance, inactive |
| `verification_status` | pending, verified, rejected |

### 3.2 Status / state columns and their allowed values

| Table | Column | Type | Allowed values (enum labels or CHECK list) |
| --- | --- | --- | --- |
| `admin_alerts` | `alert_type` | alert_type | verification_pending, payment_pending, cleaning_required, damage_reported, late_return, hold_expiring, return_due_soon, overdue, customer_issue, emergency |
| `admin_alerts` | `status` | alert_status | pending, acknowledged, resolved |
| `audit_logs` | `entity_type` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `booking_documents` | `mime_type` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `bookings` | `status` | booking_status | draft, pending, confirmed, active, completed, cancelled |
| `bookings` | `return_state` | text | `CHECK ((return_state = ANY (ARRAY['not_started'::text, 'initiated'::text, 'intake_done'::text, 'evidence_done'::text, 'issues_reviewed'::text, 'closeout_done'::text, 'deposit_processed'::text])))` |
| `bookings` | `card_type` | character varying(20) | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `bookings` | `deposit_status` | text | `CHECK ((deposit_status = ANY (ARRAY['none'::text, 'requires_payment'::text, 'authorizing'::text, 'authorized'::text, 'capturing'::text, 'captured'::text, 'releasing'::text, 'released'::text, 'failed'::text, 'expired'::text, 'canceled'::text])))` |
| `bookings` | `wl_auth_status` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `bookings` | `wl_deposit_auth_status` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `checkin_records` | `timing_status` | text | `CHECK ((timing_status = ANY (ARRAY['on_time'::text, 'early'::text, 'late'::text, 'no_show'::text])))` |
| `checkin_records` | `check_in_status` | text | `CHECK ((check_in_status = ANY (ARRAY['pending'::text, 'passed'::text, 'needs_review'::text, 'blocked'::text])))` |
| `condition_photos` | `photo_type` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `damage_reports` | `severity` | damage_severity | minor, moderate, severe |
| `damage_reports` | `status` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `delivery_status_log` | `status` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `delivery_statuses` | `status` | text | `CHECK ((status = ANY (ARRAY['unassigned'::text, 'assigned'::text, 'picked_up'::text, 'en_route'::text, 'arrived'::text, 'delivered'::text, 'issue'::text, 'cancelled'::text])))` |
| `delivery_tasks` | `status` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `deposit_jobs` | `job_type` | text | `CHECK ((job_type = ANY (ARRAY['release'::text, 'withhold'::text, 'partial_release'::text])))` |
| `deposit_jobs` | `status` | text | `CHECK ((status = ANY (ARRAY['pending'::text, 'processing'::text, 'completed'::text, 'failed'::text])))` |
| `final_invoices` | `status` | text | `CHECK ((status = ANY (ARRAY['draft'::text, 'issued'::text, 'paid'::text, 'voided'::text])))` |
| `fleet_cost_cache` | `cache_type` | text | `CHECK ((cache_type = ANY (ARRAY['unit'::text, 'category'::text])))` |
| `incident_cases` | `incident_type` | text | `CHECK ((incident_type = ANY (ARRAY['accident'::text, 'collision'::text, 'damage'::text, 'customer_damage'::text, 'weather'::text, 'mechanical'::text, 'theft'::text, 'vandalism'::text, 'glass'::text, 'tire'::text, 'hit_and_run'::text, 'other'::text])))` |
| `incident_cases` | `severity` | text | `CHECK ((severity = ANY (ARRAY['minor'::text, 'moderate'::text, 'major'::text])))` |
| `incident_cases` | `status` | text | `CHECK ((status = ANY (ARRAY['reported'::text, 'investigating'::text, 'evidence_complete'::text, 'claim_filed'::text, 'estimate'::text, 'approved'::text, 'in_repair'::text, 'resolved'::text, 'ready'::text, 'closed'::text])))` |
| `incident_repairs` | `repair_type` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `incident_repairs` | `status` | text | `CHECK ((status = ANY (ARRAY['pending'::text, 'approved'::text, 'completed'::text, 'cancelled'::text])))` |
| `maintenance_logs` | `maintenance_type` | text | `CHECK ((maintenance_type = ANY (ARRAY['scheduled_service'::text, 'repair'::text, 'inspection'::text, 'tire_replacement'::text, 'oil_change'::text, 'brake_service'::text, 'other'::text])))` |
| `notification_logs` | `notification_type` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `notification_logs` | `status` | text | `CHECK ((status = ANY (ARRAY['pending'::text, 'sent'::text, 'failed'::text])))` |
| `payment_profiles` | `card_type` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `payments` | `payment_type` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `payments` | `status` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `points_ledger` | `transaction_type` | points_transaction_type | earn, redeem, adjust, expire, reverse |
| `points_offers` | `offer_type` | offer_type | percent_off, dollar_off, free_addon, free_upgrade |
| `profiles` | `driver_license_status` | text | `CHECK ((driver_license_status = ANY (ARRAY['missing'::text, 'on_file'::text, 'expired'::text])))` |
| `profiles` | `membership_tier` | membership_tier | bronze, silver, gold, platinum |
| `profiles` | `membership_status` | membership_status | active, suspended, inactive |
| `receipts` | `status` | receipt_status | draft, issued, voided |
| `rental_agreements` | `status` | text | `CHECK ((status = ANY (ARRAY['pending'::text, 'signed'::text, 'confirmed'::text, 'voided'::text])))` |
| `rental_agreements` | `agreement_type` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `reservation_holds` | `status` | hold_status | active, expired, converted |
| `support_tickets_v2` | `status` | text | `CHECK ((status = ANY (ARRAY['new'::text, 'in_progress'::text, 'waiting_customer'::text, 'escalated'::text, 'closed'::text])))` |
| `support_tickets_v2` | `priority` | text | `CHECK ((priority = ANY (ARRAY['low'::text, 'medium'::text, 'high'::text])))` |
| `support_tickets_v2` | `created_by_type` | text | `CHECK ((created_by_type = ANY (ARRAY['customer'::text, 'admin'::text, 'support'::text, 'system'::text, 'staff'::text])))` |
| `ticket_attachments` | `file_type` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `ticket_messages_v2` | `message_type` | text | `CHECK ((message_type = ANY (ARRAY['customer_visible'::text, 'internal_note'::text])))` |
| `ticket_messages_v2` | `sender_type` | text | `CHECK ((sender_type = ANY (ARRAY['customer'::text, 'staff'::text, 'system'::text])))` |
| `ticket_timeline` | `old_status` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `ticket_timeline` | `new_status` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `tickets` | `status` | ticket_status | open, in_progress, resolved, closed, assigned, waiting_customer |
| `tickets` | `priority` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `vehicle_categories` | `fuel_type` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `vehicle_expenses` | `expense_type` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `vehicle_units` | `status` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `vehicles` | `fuel_type` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `vehicles` | `status` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `vendors` | `vendor_type` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `verification_requests` | `document_type` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |
| `verification_requests` | `status` | verification_status | pending, verified, rejected |
| `webhook_events` | `event_type` | text | no CHECK constraint — free text (UNKNOWN: allowed values not enforced in DB) |

---

## 4. RLS policies

Total policies: **194**. Expressions are verbatim from `pg_policies`.

### `abandoned_carts`

**Admin staff can delete abandoned carts** — command `DELETE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can update abandoned carts** — command `UPDATE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can view all abandoned carts** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Anyone can insert abandoned carts** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: true
```

**Anyone can read carts for session tracking** — command `SELECT`, roles `anon, authenticated`

```sql
USING: true
WITH CHECK: (none)
```

**Anyone can update carts for session tracking** — command `UPDATE`, roles `anon, authenticated`

```sql
USING: true
WITH CHECK: true
```

### `add_ons`

**Add-ons are publicly readable** — command `SELECT`, roles `public`

```sql
USING: true
WITH CHECK: (none)
```

**Admin staff can manage add-ons** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `admin_alerts`

**Admin staff can insert alerts** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Admin staff can update alerts** — command `UPDATE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can view all alerts** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `analytics_events`

**Admin and staff can read analytics** — command `SELECT`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Anyone can insert analytics events** — command `INSERT`, roles `anon, authenticated`

```sql
USING: (none)
WITH CHECK: true
```

### `audit_logs`

**Admin staff can insert audit logs** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Admin staff can view all audit logs** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can view their own audit logs** — command `SELECT`, roles `public`

```sql
USING: (user_id = auth.uid())
WITH CHECK: (none)
```

### `bank_transfer_otps`

**Admin/staff can view bank transfer OTPs** — command `SELECT`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `booking_add_ons`

**Admin staff can add booking add-ons** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Admin staff can update booking add-ons** — command `UPDATE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can view all booking add-ons** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can add add-ons to their bookings** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: (EXISTS ( SELECT 1
   FROM bookings
  WHERE ((bookings.id = booking_add_ons.booking_id) AND (bookings.user_id = auth.uid()))))
```

**Users can view their booking add-ons** — command `SELECT`, roles `public`

```sql
USING: (EXISTS ( SELECT 1
   FROM bookings
  WHERE ((bookings.id = booking_add_ons.booking_id) AND (bookings.user_id = auth.uid()))))
WITH CHECK: (none)
```

### `booking_additional_drivers`

**Staff can insert additional drivers** — command `INSERT`, roles `authenticated`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Staff can select additional drivers** — command `SELECT`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Staff can update additional drivers** — command `UPDATE`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Users can add additional drivers to their bookings** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: (booking_id IN ( SELECT bookings.id
   FROM bookings
  WHERE (bookings.user_id = auth.uid())))
```

**Users can view their own booking additional drivers** — command `SELECT`, roles `public`

```sql
USING: (booking_id IN ( SELECT bookings.id
   FROM bookings
  WHERE (bookings.user_id = auth.uid())))
WITH CHECK: (none)
```

### `booking_documents`

**Staff can add booking documents in their branch** — command `INSERT`, roles `authenticated`

```sql
USING: (none)
WITH CHECK: (is_super_admin(auth.uid()) OR (is_active_staff(auth.uid()) AND (EXISTS ( SELECT 1
   FROM bookings b
  WHERE ((b.id = booking_documents.booking_id) AND can_access_location(auth.uid(), b.location_id))))))
```

**Staff can update booking documents in their branch** — command `UPDATE`, roles `authenticated`

```sql
USING: (is_super_admin(auth.uid()) OR (is_active_staff(auth.uid()) AND (EXISTS ( SELECT 1
   FROM bookings b
  WHERE ((b.id = booking_documents.booking_id) AND can_access_location(auth.uid(), b.location_id))))))
WITH CHECK: (none)
```

**Staff can view booking documents in their branch** — command `SELECT`, roles `authenticated`

```sql
USING: (is_super_admin(auth.uid()) OR (is_active_staff(auth.uid()) AND (EXISTS ( SELECT 1
   FROM bookings b
  WHERE ((b.id = booking_documents.booking_id) AND can_access_location(auth.uid(), b.location_id))))))
WITH CHECK: (none)
```

### `booking_extensions`

**Admin staff can view booking extensions** — command `SELECT`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `booking_otps`

**Users can view their own OTPs** — command `SELECT`, roles `public`

```sql
USING: (auth.uid() = user_id)
WITH CHECK: (none)
```

### `bookings`

**Admin staff can update all bookings** — command `UPDATE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can view all bookings** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Drivers can view assigned bookings** — command `SELECT`, roles `public`

```sql
USING: (auth.uid() = assigned_driver_id)
WITH CHECK: (none)
```

**Drivers can view delivery pool bookings** — command `SELECT`, roles `public`

```sql
USING: (has_role(auth.uid(), 'driver'::app_role) AND (pickup_address IS NOT NULL) AND (status = ANY (ARRAY['pending'::booking_status, 'confirmed'::booking_status, 'active'::booking_status])))
WITH CHECK: (none)
```

**Users can view their own bookings** — command `SELECT`, roles `public`

```sql
USING: (auth.uid() = user_id)
WITH CHECK: (none)
```

### `checkin_records`

**Staff can insert check-in records** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Staff can update check-in records** — command `UPDATE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Staff can view all check-in records** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `competitor_pricing`

**Admins can delete competitor pricing** — command `DELETE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admins can insert competitor pricing** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Admins can update competitor pricing** — command `UPDATE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admins can view competitor pricing** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `condition_photos`

**Admin staff can view all condition photos** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can add photos to their bookings** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: (auth.uid() = captured_by)
```

**Users can view their booking photos** — command `SELECT`, roles `public`

```sql
USING: (EXISTS ( SELECT 1
   FROM bookings
  WHERE ((bookings.id = condition_photos.booking_id) AND (bookings.user_id = auth.uid()))))
WITH CHECK: (none)
```

### `customers`

**Staff can manage customers** — command `ALL`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: is_admin_or_staff(auth.uid())
```

### `damage_reports`

**Admin staff can view all damages** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Drivers can create damage reports** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: ((EXISTS ( SELECT 1
   FROM bookings b
  WHERE ((b.id = damage_reports.booking_id) AND (b.assigned_driver_id = auth.uid())))) OR is_admin_or_staff(auth.uid()))
```

**Staff can insert damage reports** — command `INSERT`, roles `authenticated`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Staff can update damage reports** — command `UPDATE`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Users can view their booking damages** — command `SELECT`, roles `public`

```sql
USING: (EXISTS ( SELECT 1
   FROM bookings
  WHERE ((bookings.id = damage_reports.booking_id) AND (bookings.user_id = auth.uid()))))
WITH CHECK: (none)
```

### `delivery_status_log`

**Staff can insert delivery status logs** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Staff can view delivery status logs** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `delivery_statuses`

**Admins can delete delivery status** — command `DELETE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Drivers can insert delivery status** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: ((EXISTS ( SELECT 1
   FROM bookings b
  WHERE ((b.id = delivery_statuses.booking_id) AND (b.assigned_driver_id = auth.uid())))) OR is_admin_or_staff(auth.uid()))
```

**Drivers can update their delivery status** — command `UPDATE`, roles `public`

```sql
USING: ((EXISTS ( SELECT 1
   FROM bookings b
  WHERE ((b.id = delivery_statuses.booking_id) AND (b.assigned_driver_id = auth.uid())))) OR is_admin_or_staff(auth.uid()))
WITH CHECK: (none)
```

**Drivers can view delivery pool statuses** — command `SELECT`, roles `public`

```sql
USING: (has_role(auth.uid(), 'driver'::app_role) AND (EXISTS ( SELECT 1
   FROM bookings b
  WHERE ((b.id = delivery_statuses.booking_id) AND (b.pickup_address IS NOT NULL) AND (b.status = ANY (ARRAY['pending'::booking_status, 'confirmed'::booking_status, 'active'::booking_status]))))))
WITH CHECK: (none)
```

**Drivers can view their deliveries** — command `SELECT`, roles `public`

```sql
USING: ((EXISTS ( SELECT 1
   FROM bookings b
  WHERE ((b.id = delivery_statuses.booking_id) AND (b.assigned_driver_id = auth.uid())))) OR is_admin_or_staff(auth.uid()))
WITH CHECK: (none)
```

**Staff can view all delivery statuses** — command `SELECT`, roles `public`

```sql
USING: (is_admin_or_staff(auth.uid()) AND (EXISTS ( SELECT 1
   FROM bookings b
  WHERE ((b.id = delivery_statuses.booking_id) AND (b.pickup_address IS NOT NULL)))))
WITH CHECK: (none)
```

### `delivery_tasks`

**Staff and admin can insert delivery tasks** — command `INSERT`, roles `authenticated`

```sql
USING: (none)
WITH CHECK: (has_role(auth.uid(), 'admin'::app_role) OR has_role(auth.uid(), 'staff'::app_role))
```

**Staff and admin can view delivery tasks** — command `SELECT`, roles `authenticated`

```sql
USING: (has_role(auth.uid(), 'admin'::app_role) OR has_role(auth.uid(), 'staff'::app_role) OR has_role(auth.uid(), 'driver'::app_role))
WITH CHECK: (none)
```

**Staff, admin, and drivers can update delivery tasks** — command `UPDATE`, roles `authenticated`

```sql
USING: (has_role(auth.uid(), 'admin'::app_role) OR has_role(auth.uid(), 'staff'::app_role) OR (has_role(auth.uid(), 'driver'::app_role) AND (assigned_driver_id = auth.uid())))
WITH CHECK: (none)
```

### `deposit_jobs`

**Staff can manage deposit jobs** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Staff can view deposit jobs** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `deposit_ledger`

**Admin staff can view all deposit ledger entries** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Staff can insert deposit ledger** — command `INSERT`, roles `authenticated`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Staff can update deposit ledger** — command `UPDATE`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Users can view their booking deposit ledger** — command `SELECT`, roles `public`

```sql
USING: (EXISTS ( SELECT 1
   FROM bookings
  WHERE ((bookings.id = deposit_ledger.booking_id) AND (bookings.user_id = auth.uid()))))
WITH CHECK: (none)
```

### `final_invoices`

**Staff can create final invoices** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Staff can update final invoices** — command `UPDATE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Staff can view all final invoices** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can view own final invoices** — command `SELECT`, roles `public`

```sql
USING: (EXISTS ( SELECT 1
   FROM bookings b
  WHERE ((b.id = final_invoices.booking_id) AND (b.user_id = auth.uid()))))
WITH CHECK: (none)
```

### `fleet_cost_cache`

**Admin staff can manage cost cache** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `incident_cases`

**Staff can insert incident cases** — command `INSERT`, roles `authenticated`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Staff can select incident cases** — command `SELECT`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Staff can update incident cases** — command `UPDATE`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: is_admin_or_staff(auth.uid())
```

### `incident_photos`

**Admin staff can manage incident photos** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `incident_repairs`

**Staff can insert incident repairs** — command `INSERT`, roles `authenticated`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Staff can select incident repairs** — command `SELECT`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Staff can update incident repairs** — command `UPDATE`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: is_admin_or_staff(auth.uid())
```

### `inspection_metrics`

**Admin staff can manage inspections** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can view all inspections** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can view their booking inspections** — command `SELECT`, roles `public`

```sql
USING: (EXISTS ( SELECT 1
   FROM bookings
  WHERE ((bookings.id = inspection_metrics.booking_id) AND (bookings.user_id = auth.uid()))))
WITH CHECK: (none)
```

### `locations`

**Admin staff can manage locations** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Locations are publicly readable** — command `SELECT`, roles `public`

```sql
USING: true
WITH CHECK: (none)
```

### `maintenance_logs`

**Staff can insert maintenance logs** — command `INSERT`, roles `authenticated`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Staff can select maintenance logs** — command `SELECT`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Staff can update maintenance logs** — command `UPDATE`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: is_admin_or_staff(auth.uid())
```

### `membership_tiers`

**Admins can modify tiers** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Anyone can view tiers** — command `SELECT`, roles `public`

```sql
USING: true
WITH CHECK: (none)
```

### `notification_logs`

**Admin staff can view all notification logs** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can view their own notification logs** — command `SELECT`, roles `public`

```sql
USING: (auth.uid() = user_id)
WITH CHECK: (none)
```

### `offer_redemptions`

**Staff can manage redemptions** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can view own redemptions** — command `SELECT`, roles `public`

```sql
USING: (auth.uid() = user_id)
WITH CHECK: (none)
```

### `payment_profiles`

**Service role manages payment profiles** — command `ALL`, roles `service_role`

```sql
USING: true
WITH CHECK: true
```

**Users can view own payment profiles** — command `SELECT`, roles `authenticated`

```sql
USING: (user_id = auth.uid())
WITH CHECK: (none)
```

### `payments`

**Admin staff can insert payments** — command `INSERT`, roles `authenticated`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Admin staff can update payments** — command `UPDATE`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Admin staff can view all payments** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can view their own payments** — command `SELECT`, roles `public`

```sql
USING: (auth.uid() = user_id)
WITH CHECK: (none)
```

### `points_ledger`

**Staff can insert points** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Staff can view all points** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can view own points** — command `SELECT`, roles `public`

```sql
USING: (auth.uid() = user_id)
WITH CHECK: (none)
```

### `points_offers`

**Anyone can view active offers** — command `SELECT`, roles `public`

```sql
USING: ((is_active = true) OR is_admin_or_staff(auth.uid()))
WITH CHECK: (none)
```

**Staff can manage offers** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `points_settings`

**Anyone can read points settings** — command `SELECT`, roles `public`

```sql
USING: true
WITH CHECK: (none)
```

**Staff can modify points settings** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `profiles`

**Admin staff can update profiles** — command `UPDATE`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Admin staff can view all profiles** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Drivers can view delivery customer profiles** — command `SELECT`, roles `public`

```sql
USING: (has_role(auth.uid(), 'driver'::app_role) AND (EXISTS ( SELECT 1
   FROM bookings b
  WHERE ((b.user_id = profiles.id) AND (b.pickup_address IS NOT NULL) AND (b.status = ANY (ARRAY['pending'::booking_status, 'confirmed'::booking_status, 'active'::booking_status]))))))
WITH CHECK: (none)
```

**Users can insert their own profile** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: (auth.uid() = id)
```

**Users can update their own profile** — command `UPDATE`, roles `public`

```sql
USING: (auth.uid() = id)
WITH CHECK: (none)
```

**Users can view their own profile** — command `SELECT`, roles `public`

```sql
USING: (auth.uid() = id)
WITH CHECK: (none)
```

### `receipt_events`

**Admin staff can insert receipt events** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Admin staff can view all receipt events** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can view events for their receipts** — command `SELECT`, roles `public`

```sql
USING: (EXISTS ( SELECT 1
   FROM (receipts r
     JOIN bookings b ON ((b.id = r.booking_id)))
  WHERE ((r.id = receipt_events.receipt_id) AND (b.user_id = auth.uid()) AND (r.status = 'issued'::receipt_status))))
WITH CHECK: (none)
```

### `receipts`

**Admin staff can manage receipts** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can view all receipts** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can view their issued receipts** — command `SELECT`, roles `public`

```sql
USING: ((status = 'issued'::receipt_status) AND (EXISTS ( SELECT 1
   FROM bookings
  WHERE ((bookings.id = receipts.booking_id) AND (bookings.user_id = auth.uid())))))
WITH CHECK: (none)
```

### `rental_agreements`

**Admin staff can manage agreements** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can view all agreements** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can sign their booking agreements** — command `UPDATE`, roles `public`

```sql
USING: (EXISTS ( SELECT 1
   FROM bookings
  WHERE ((bookings.id = rental_agreements.booking_id) AND (bookings.user_id = auth.uid()))))
WITH CHECK: (EXISTS ( SELECT 1
   FROM bookings
  WHERE ((bookings.id = rental_agreements.booking_id) AND (bookings.user_id = auth.uid()))))
```

**Users can view their booking agreements** — command `SELECT`, roles `public`

```sql
USING: (EXISTS ( SELECT 1
   FROM bookings
  WHERE ((bookings.id = rental_agreements.booking_id) AND (bookings.user_id = auth.uid()))))
WITH CHECK: (none)
```

### `reservation_holds`

**Admin staff can view all holds** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can create their own holds** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: (auth.uid() = user_id)
```

**Users can update their own holds** — command `UPDATE`, roles `public`

```sql
USING: (auth.uid() = user_id)
WITH CHECK: (none)
```

**Users can view their own holds** — command `SELECT`, roles `public`

```sql
USING: (auth.uid() = user_id)
WITH CHECK: (none)
```

### `staff_assignments`

**Active staff can view staff identities** — command `SELECT`, roles `authenticated`

```sql
USING: is_active_staff(auth.uid())
WITH CHECK: (none)
```

**Staff can view their own assignment** — command `SELECT`, roles `authenticated`

```sql
USING: (user_id = auth.uid())
WITH CHECK: (none)
```

**Super admins manage staff assignments** — command `ALL`, roles `authenticated`

```sql
USING: is_super_admin(auth.uid())
WITH CHECK: is_super_admin(auth.uid())
```

### `support_macros`

**Staff can manage macros** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `support_tickets_v2`

**Customers can create tickets** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: ((auth.uid() = customer_id) OR (auth.uid() = created_by))
```

**Customers can update their tickets** — command `UPDATE`, roles `public`

```sql
USING: (auth.uid() = customer_id)
WITH CHECK: (none)
```

**Customers can view their tickets** — command `SELECT`, roles `public`

```sql
USING: (auth.uid() = customer_id)
WITH CHECK: (none)
```

**Staff can manage tickets** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Staff can view all tickets** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `system_settings`

**Admins can manage settings** — command `ALL`, roles `authenticated`

```sql
USING: has_role(auth.uid(), 'admin'::app_role)
WITH CHECK: has_role(auth.uid(), 'admin'::app_role)
```

**Settings are viewable by everyone** — command `SELECT`, roles `public`

```sql
USING: true
WITH CHECK: (none)
```

### `ticket_attachments`

**Admin staff can insert attachments** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Admin staff can view all attachments** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can add attachments to their tickets** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: (EXISTS ( SELECT 1
   FROM tickets
  WHERE ((tickets.id = ticket_attachments.ticket_id) AND (tickets.user_id = auth.uid()))))
```

**Users can view non-internal attachments for their tickets** — command `SELECT`, roles `public`

```sql
USING: ((is_internal = false) AND (EXISTS ( SELECT 1
   FROM tickets
  WHERE ((tickets.id = ticket_attachments.ticket_id) AND (tickets.user_id = auth.uid())))))
WITH CHECK: (none)
```

### `ticket_audit_log`

**Staff can insert audit logs** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: (is_admin_or_staff(auth.uid()) OR (auth.uid() = performed_by))
```

**Staff can view ticket audit logs** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `ticket_messages`

**Admin staff can add messages** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Admin staff can view all messages** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can add messages to their tickets** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: (EXISTS ( SELECT 1
   FROM tickets
  WHERE ((tickets.id = ticket_messages.ticket_id) AND (tickets.user_id = auth.uid()))))
```

**Users can view messages on their tickets** — command `SELECT`, roles `public`

```sql
USING: (EXISTS ( SELECT 1
   FROM tickets
  WHERE ((tickets.id = ticket_messages.ticket_id) AND (tickets.user_id = auth.uid()))))
WITH CHECK: (none)
```

### `ticket_messages_v2`

**Customers can add messages to their tickets** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: ((message_type = 'customer_visible'::text) AND (sender_id = auth.uid()) AND (EXISTS ( SELECT 1
   FROM support_tickets_v2 t
  WHERE ((t.id = ticket_messages_v2.ticket_id) AND (t.customer_id = auth.uid())))))
```

**Customers can view their customer-visible messages** — command `SELECT`, roles `public`

```sql
USING: ((message_type = 'customer_visible'::text) AND (EXISTS ( SELECT 1
   FROM support_tickets_v2 t
  WHERE ((t.id = ticket_messages_v2.ticket_id) AND (t.customer_id = auth.uid())))))
WITH CHECK: (none)
```

**Staff can manage messages** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Staff can view all messages** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `ticket_timeline`

**Admin staff can insert timeline entries** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Admin staff can view all timeline entries** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can view timeline for their tickets** — command `SELECT`, roles `public`

```sql
USING: (EXISTS ( SELECT 1
   FROM tickets
  WHERE ((tickets.id = ticket_timeline.ticket_id) AND (tickets.user_id = auth.uid()))))
WITH CHECK: (none)
```

### `tickets`

**Admin staff can update all tickets** — command `UPDATE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can view all tickets** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can create their own tickets** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: (auth.uid() = user_id)
```

**Users can update their own tickets** — command `UPDATE`, roles `public`

```sql
USING: (auth.uid() = user_id)
WITH CHECK: (none)
```

**Users can view their own tickets** — command `SELECT`, roles `public`

```sql
USING: (auth.uid() = user_id)
WITH CHECK: (none)
```

### `user_roles`

**Admins can manage all roles** — command `ALL`, roles `public`

```sql
USING: has_role(auth.uid(), 'admin'::app_role)
WITH CHECK: (none)
```

**Users can view their own roles** — command `SELECT`, roles `public`

```sql
USING: (auth.uid() = user_id)
WITH CHECK: (none)
```

### `vehicle_categories`

**Admin staff can manage categories** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can view categories** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Categories are publicly readable** — command `SELECT`, roles `public`

```sql
USING: (is_active = true)
WITH CHECK: (none)
```

### `vehicle_expenses`

**Admin staff can delete expenses** — command `DELETE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can insert expenses** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Admin staff can update expenses** — command `UPDATE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can view all expenses** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `vehicle_swap_history`

**Admin/staff can view vehicle swap history** — command `SELECT`, roles `authenticated`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `vehicle_units`

**Admin staff can delete vehicle units** — command `DELETE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can insert vehicle units** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Admin staff can update vehicle units** — command `UPDATE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can view all vehicle units** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Drivers can view delivery vehicle units** — command `SELECT`, roles `public`

```sql
USING: (has_role(auth.uid(), 'driver'::app_role) AND (EXISTS ( SELECT 1
   FROM bookings b
  WHERE ((b.assigned_unit_id = vehicle_units.id) AND (b.pickup_address IS NOT NULL) AND (b.status = ANY (ARRAY['pending'::booking_status, 'confirmed'::booking_status, 'active'::booking_status]))))))
WITH CHECK: (none)
```

### `vehicles`

**Admin staff can insert vehicles** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Admin staff can update vehicles** — command `UPDATE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Vehicles are publicly readable** — command `SELECT`, roles `public`

```sql
USING: true
WITH CHECK: (none)
```

### `vendors`

**Admin staff can delete vendors** — command `DELETE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can insert vendors** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: is_admin_or_staff(auth.uid())
```

**Admin staff can update vendors** — command `UPDATE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can view vendors** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

### `verification_requests`

**Admin staff can update verifications** — command `UPDATE`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can view all verifications** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can create their own verifications** — command `INSERT`, roles `public`

```sql
USING: (none)
WITH CHECK: (auth.uid() = user_id)
```

**Users can delete their own pending verifications** — command `DELETE`, roles `authenticated`

```sql
USING: ((auth.uid() = user_id) AND (status = 'pending'::verification_status))
WITH CHECK: (none)
```

**Users can view their own verifications** — command `SELECT`, roles `public`

```sql
USING: (auth.uid() = user_id)
WITH CHECK: (none)
```

### `walkaround_inspections`

**Admin staff can manage walkarounds** — command `ALL`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Admin staff can view all walkarounds** — command `SELECT`, roles `public`

```sql
USING: is_admin_or_staff(auth.uid())
WITH CHECK: (none)
```

**Users can acknowledge walkarounds** — command `UPDATE`, roles `public`

```sql
USING: (EXISTS ( SELECT 1
   FROM bookings
  WHERE ((bookings.id = walkaround_inspections.booking_id) AND (bookings.user_id = auth.uid()))))
WITH CHECK: (EXISTS ( SELECT 1
   FROM bookings
  WHERE ((bookings.id = walkaround_inspections.booking_id) AND (bookings.user_id = auth.uid()))))
```

**Users can view their booking walkarounds** — command `SELECT`, roles `public`

```sql
USING: (EXISTS ( SELECT 1
   FROM bookings
  WHERE ((bookings.id = walkaround_inspections.booking_id) AND (bookings.user_id = auth.uid()))))
WITH CHECK: (none)
```

### `webhook_events`

**Service role only webhook events** — command `ALL`, roles `service_role`

```sql
USING: true
WITH CHECK: true
```

**Tables with RLS enabled but ZERO policies (no client access at all, service_role only):** `booking_access_tokens`, `rate_limits`

---

## 5. Database functions and RPCs

Total functions in `public`: **41**.

### `assign_vin_to_booking(p_category_id uuid, p_booking_id uuid, p_location_id uuid)`

- **Returns:** `uuid`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** `vehicle_units`
- **Tables written:** `bookings`, `vehicle_units`

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
  v_unit_id UUID;
BEGIN
  -- Lock and select an available unit from the category at the location
  SELECT id INTO v_unit_id
  FROM vehicle_units
  WHERE category_id = p_category_id
    AND location_id = p_location_id
    AND status = 'available'
  ORDER BY created_at ASC
  FOR UPDATE SKIP LOCKED
  LIMIT 1;
  
  IF v_unit_id IS NULL THEN
    RAISE EXCEPTION 'No available units in this category at this location';
  END IF;
  
  -- Mark the unit as on_rent
  UPDATE vehicle_units
  SET status = 'on_rent',
      updated_at = now()
  WHERE id = v_unit_id;
  
  -- Assign to booking
  UPDATE bookings
  SET assigned_unit_id = v_unit_id,
      updated_at = now()
  WHERE id = p_booking_id;
  
  RETURN v_unit_id;
END;
```

### `auto_create_damage_ticket()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** `bookings`
- **Tables written:** `support_tickets_v2`

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
  v_customer_id UUID;
BEGIN
  SELECT user_id INTO v_customer_id 
  FROM public.bookings 
  WHERE id = NEW.booking_id;

  INSERT INTO public.support_tickets_v2 (
    subject,
    description,
    category,
    priority,
    is_urgent,
    booking_id,
    damage_id,
    customer_id,
    created_by,
    created_by_type,
    status
  ) VALUES (
    'Damage Report: ' || NEW.severity || ' - ' || NEW.location_on_vehicle,
    NEW.description,
    'damage',
    CASE WHEN NEW.severity = 'severe' THEN 'high' ELSE 'medium' END,
    NEW.severity = 'severe',
    NEW.booking_id,
    NEW.id,
    v_customer_id,
    NEW.reported_by,
    'staff',
    'new'
  );
  
  RETURN NEW;
END;
```

### `auto_create_incident_ticket()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** `bookings`
- **Tables written:** `support_tickets_v2`

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
  v_customer_id UUID;
BEGIN
  -- Get customer ID from booking (if exists)
  IF NEW.booking_id IS NOT NULL THEN
    SELECT user_id INTO v_customer_id 
    FROM public.bookings 
    WHERE id = NEW.booking_id;
  ELSE
    v_customer_id := NEW.customer_id;
  END IF;

  -- Create support ticket linked to incident
  INSERT INTO public.support_tickets_v2 (
    subject,
    description,
    category,
    priority,
    is_urgent,
    booking_id,
    incident_id,
    customer_id,
    created_by,
    created_by_type,
    status
  ) VALUES (
    'Incident: ' || INITCAP(REPLACE(NEW.incident_type, '_', ' ')) || 
    ' - ' || INITCAP(NEW.severity),
    NEW.description,
    'incident',
    CASE 
      WHEN NEW.severity = 'major' THEN 'high'
      WHEN NEW.severity = 'moderate' THEN 'medium'
      ELSE 'low'
    END,
    NEW.severity = 'major' OR COALESCE(NOT NEW.is_drivable, false),
    NEW.booking_id,
    NEW.id,
    v_customer_id,
    NEW.created_by,
    'staff',
    'new'
  );
  
  RETURN NEW;
END;
```

### `auto_reopen_ticket_on_reply()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY INVOKER
- **Tables read:** none
- **Tables written:** `support_tickets_v2`

**Body (verbatim — every statement in execution order):**

```sql
BEGIN
  IF NEW.sender_type = 'customer' AND NEW.message_type = 'customer_visible' THEN
    UPDATE public.support_tickets_v2
    SET status = 'in_progress', updated_at = now()
    WHERE id = NEW.ticket_id AND status = 'closed';
  END IF;
  RETURN NEW;
END;
```

### `block_sensitive_booking_inserts()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY INVOKER
- **Tables read:** none
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
  jwt_role text := current_setting('request.jwt.claim.role', true);
  pg_role text := current_setting('role', true);
  sess_role text := session_user;
BEGIN
  IF jwt_role = 'service_role' 
     OR pg_role = 'service_role' 
     OR pg_role = 'supabase_admin' 
     OR pg_role = 'postgres'
     OR sess_role = 'service_role'
     OR sess_role = 'supabase_admin'
     OR sess_role = 'postgres'
     OR current_user = 'service_role'
     OR current_user = 'supabase_admin'
     OR current_user = 'postgres'
  THEN
    RETURN NEW;
  END IF;

  IF (
    NEW.status IS DISTINCT FROM 'draft'
    OR COALESCE(NEW.subtotal, 0) != 0
    OR COALESCE(NEW.tax_amount, 0) != 0
    OR COALESCE(NEW.total_amount, 0) != 0
    OR COALESCE(NEW.deposit_amount, 0) != 0
    OR COALESCE(NEW.delivery_fee, 0) != 0
    OR COALESCE(NEW.different_dropoff_fee, 0) != 0
    OR COALESCE(NEW.upgrade_daily_fee, 0) != 0
    OR COALESCE(NEW.young_driver_fee, 0) != 0
    OR COALESCE(NEW.weekend_surcharge, 0) != 0
    OR COALESCE(NEW.duration_discount, 0) != 0
    OR COALESCE(NEW.daily_rate, 0) != 0
  ) THEN
    RAISE EXCEPTION 'Booking financial fields can only be set via service_role (jwt=%, pg=%, sess=%, cur=%)', jwt_role, pg_role, sess_role, current_user;
  END IF;

  RETURN NEW;
END;
```

### `block_sensitive_booking_updates()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** none
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
  jwt_role text := current_setting('request.jwt.claim.role', true);
  pg_role text := current_setting('role', true);
  sess_role text := session_user;
BEGIN
  IF jwt_role = 'service_role'
     OR pg_role = 'service_role'
     OR pg_role = 'supabase_admin'
     OR pg_role = 'postgres'
     OR sess_role = 'service_role'
     OR sess_role = 'supabase_admin'
     OR sess_role = 'postgres'
     OR current_user = 'service_role'
     OR current_user = 'supabase_admin'
     OR current_user = 'postgres'
  THEN
    RETURN NEW;
  END IF;

  IF (
    NEW.status IS DISTINCT FROM OLD.status
    OR NEW.subtotal IS DISTINCT FROM OLD.subtotal
    OR NEW.tax_amount IS DISTINCT FROM OLD.tax_amount
    OR NEW.total_amount IS DISTINCT FROM OLD.total_amount
    OR NEW.deposit_amount IS DISTINCT FROM OLD.deposit_amount
    OR NEW.delivery_fee IS DISTINCT FROM OLD.delivery_fee
    OR NEW.different_dropoff_fee IS DISTINCT FROM OLD.different_dropoff_fee
    OR NEW.upgrade_daily_fee IS DISTINCT FROM OLD.upgrade_daily_fee
    OR NEW.young_driver_fee IS DISTINCT FROM OLD.young_driver_fee
    OR NEW.weekend_surcharge IS DISTINCT FROM OLD.weekend_surcharge
    OR NEW.duration_discount IS DISTINCT FROM OLD.duration_discount
    OR NEW.daily_rate IS DISTINCT FROM OLD.daily_rate
    OR NEW.processing_fee IS DISTINCT FROM OLD.processing_fee
    OR NEW.processing_fee_rate IS DISTINCT FROM OLD.processing_fee_rate
    OR NEW.paid_offline IS DISTINCT FROM OLD.paid_offline
    OR NEW.offline_payment_method IS DISTINCT FROM OLD.offline_payment_method
    OR NEW.offline_payment_reference IS DISTINCT FROM OLD.offline_payment_reference
    OR NEW.offline_paid_at IS DISTINCT FROM OLD.offline_paid_at
    OR NEW.offline_paid_by IS DISTINCT FROM OLD.offline_paid_by
  ) THEN
    RAISE EXCEPTION 'Client updates to booking financial/status fields are not allowed (jwt=%, pg=%, sess=%, cur=%)', jwt_role, pg_role, sess_role, current_user;
  END IF;

  RETURN NEW;
END;
```

### `can_access_location(_user_id uuid, _location_id uuid)`

- **Returns:** `boolean`
- **Language:** sql
- **Security:** SECURITY DEFINER
- **Tables read:** `staff_assignments`
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
SELECT CASE
    WHEN public.is_super_admin(_user_id) THEN true
    WHEN _location_id IS NULL THEN false
    ELSE EXISTS (
      SELECT 1 FROM public.staff_assignments
      WHERE user_id = _user_id
        AND is_active = true
        AND location_id = _location_id
    )
  END
```

### `check_category_availability(p_category_id uuid, p_location_id uuid, p_start_at timestamp with time zone, p_end_at timestamp with time zone, p_exclude_hold uuid DEFAULT NULL::uuid, p_exclude_booking uuid DEFAULT NULL::uuid)`

- **Returns:** `TABLE(available boolean, available_count bigint, total_count bigint)`
- **Language:** sql
- **Security:** SECURITY DEFINER
- **Tables read:** none
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
SELECT
    COALESCE(a.available_count, 0) > 0,
    COALESCE(a.available_count, 0),
    COALESCE(a.total_count, 0)
  FROM (
    SELECT * FROM public.get_category_availability(
      p_location_id, p_start_at, p_end_at, p_exclude_hold, p_exclude_booking
    )
  ) a
  WHERE a.id = p_category_id;
```

### `check_rate_limit(p_key text, p_window_seconds integer, p_max_requests integer)`

- **Returns:** `TABLE(allowed boolean, remaining integer, reset_at timestamp with time zone)`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** none
- **Tables written:** `rate_limits`

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
  v_now TIMESTAMPTZ := now();
  v_window_start TIMESTAMPTZ;
  v_count INT;
  v_reset TIMESTAMPTZ;
BEGIN
  -- Atomic upsert + check in single statement
  INSERT INTO public.rate_limits (key, window_start, request_count, window_seconds, max_requests)
  VALUES (p_key, v_now, 1, p_window_seconds, p_max_requests)
  ON CONFLICT (key) DO UPDATE SET
    -- Reset window if expired, otherwise increment
    window_start = CASE
      WHEN rate_limits.window_start + (rate_limits.window_seconds * interval '1 second') < v_now
      THEN v_now
      ELSE rate_limits.window_start
    END,
    request_count = CASE
      WHEN rate_limits.window_start + (rate_limits.window_seconds * interval '1 second') < v_now
      THEN 1
      ELSE rate_limits.request_count + 1
    END,
    window_seconds = p_window_seconds,
    max_requests = p_max_requests
  RETURNING
    rate_limits.request_count <= p_max_requests,
    GREATEST(p_max_requests - rate_limits.request_count, 0),
    rate_limits.window_start + (p_window_seconds * interval '1 second')
  INTO allowed, remaining, reset_at;

  RETURN NEXT;
END;
```

### `check_ticket_escalation()`

- **Returns:** `void`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** `admin_alerts`, `tickets`
- **Tables written:** `admin_alerts`, `tickets`

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
  escalation_hours_normal INTEGER := 24;
  escalation_hours_high INTEGER := 12;
  alert_hours_urgent INTEGER := 4;
  tickets_escalated INTEGER := 0;
  current_ticket RECORD;
BEGIN
  -- Escalate normal -> high (after 24 hours)
  FOR current_ticket IN 
    SELECT id, subject, booking_id, user_id
    FROM tickets
    WHERE status IN ('open', 'in_progress')
    AND priority = 'normal'
    AND updated_at < NOW() - INTERVAL '24 hours'
    AND (escalated_at IS NULL OR escalated_at < NOW() - INTERVAL '24 hours')
  LOOP
    UPDATE tickets SET
      priority = 'high',
      escalated_at = NOW(),
      escalated_from = 'normal',
      escalation_count = COALESCE(escalation_count, 0) + 1,
      updated_at = NOW()
    WHERE id = current_ticket.id;
    
    tickets_escalated := tickets_escalated + 1;
  END LOOP;
  
  -- Escalate high -> urgent (after 12 hours)
  FOR current_ticket IN 
    SELECT id, subject, booking_id, user_id
    FROM tickets
    WHERE status IN ('open', 'in_progress')
    AND priority = 'high'
    AND updated_at < NOW() - INTERVAL '12 hours'
    AND (escalated_at IS NULL OR escalated_at < NOW() - INTERVAL '12 hours')
  LOOP
    UPDATE tickets SET
      priority = 'urgent',
      escalated_at = NOW(),
      escalated_from = 'high',
      escalation_count = COALESCE(escalation_count, 0) + 1,
      updated_at = NOW()
    WHERE id = current_ticket.id;
    
    tickets_escalated := tickets_escalated + 1;
  END LOOP;
  
  -- Create alerts for urgent tickets unresolved > 4 hours
  INSERT INTO admin_alerts (alert_type, title, message, booking_id, user_id, status)
  SELECT 
    'verification_pending'::alert_type,
    '⚠️ Urgent ticket needs attention: ' || LEFT(t.subject, 40),
    'Ticket has been urgent priority for over 4 hours without resolution.',
    t.booking_id,
    t.user_id,
    'pending'::alert_status
  FROM tickets t
  WHERE t.status IN ('open', 'in_progress')
  AND t.priority = 'urgent'
  AND t.updated_at < NOW() - INTERVAL '4 hours'
  AND NOT EXISTS (
    SELECT 1 FROM admin_alerts a 
    WHERE a.booking_id = t.booking_id 
    AND a.alert_type = 'verification_pending'
    AND a.title ILIKE '%urgent ticket%'
    AND a.created_at > NOW() - INTERVAL '24 hours'
  );

  RAISE NOTICE 'Ticket escalation complete. Tickets escalated: %', tickets_escalated;
END;
```

### `cleanup_expired_rate_limits()`

- **Returns:** `void`
- **Language:** sql
- **Security:** SECURITY INVOKER
- **Tables read:** `rate_limits`
- **Tables written:** `rate_limits`

**Body (verbatim — every statement in execution order):**

```sql
DELETE FROM public.rate_limits
  WHERE window_start + (window_seconds || ' seconds')::interval < now();
```

### `create_alert_for_new_ticket()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** `bookings`, `profiles`
- **Tables written:** `admin_alerts`

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
  v_code text;
  v_name text;
BEGIN
  IF NEW.created_by_type <> 'customer' THEN
    RETURN NEW;
  END IF;

  SELECT b.booking_code INTO v_code FROM public.bookings b WHERE b.id = NEW.booking_id;
  SELECT p.full_name INTO v_name FROM public.profiles p WHERE p.id = NEW.customer_id;

  INSERT INTO public.admin_alerts (
    alert_type, title, message, booking_id, ticket_id, user_id, status, expires_at
  ) VALUES (
    CASE WHEN NEW.is_urgent OR NEW.priority = 'high' THEN 'emergency'::alert_type
         ELSE 'customer_issue'::alert_type END,
    'Support ticket ' || NEW.ticket_id || ': ' || LEFT(NEW.subject, 60),
    COALESCE(v_name, COALESCE(NEW.guest_name, 'Customer'))
      || COALESCE(' · Booking ' || v_code, '')
      || ' · ' || LEFT(COALESCE(NEW.description, ''), 200),
    NEW.booking_id,
    NEW.id,
    NEW.customer_id,
    'pending'::alert_status,
    NULL
  );

  RETURN NEW;
END;
```

### `create_delivery_status_for_booking()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** `delivery_statuses`
- **Tables written:** `delivery_statuses`

**Body (verbatim — every statement in execution order):**

```sql
BEGIN
  -- Only create delivery status for bookings with a delivery address (delivery bookings)
  IF NEW.pickup_address IS NOT NULL THEN
    -- Check if delivery_status already exists (for idempotency)
    IF NOT EXISTS (SELECT 1 FROM public.delivery_statuses WHERE booking_id = NEW.id) THEN
      INSERT INTO public.delivery_statuses (
        booking_id,
        status,
        updated_by,
        created_at,
        updated_at
      ) VALUES (
        NEW.id,
        CASE WHEN NEW.assigned_driver_id IS NOT NULL THEN 'assigned' ELSE 'unassigned' END,
        NEW.assigned_driver_id,
        now(),
        now()
      );
    END IF;
  END IF;
  
  RETURN NEW;
END;
```

### `enforce_addon_price_integrity()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** none
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
  jwt_role text := current_setting('request.jwt.claim.role', true);
  pg_role text := current_setting('role', true);
  sess_role text := session_user;
BEGIN
  -- Allow if ANY role indicator matches service_role / admin
  IF jwt_role = 'service_role' 
     OR pg_role = 'service_role' 
     OR pg_role = 'supabase_admin' 
     OR pg_role = 'postgres'
     OR sess_role = 'service_role'
     OR sess_role = 'supabase_admin'
     OR sess_role = 'postgres'
     OR current_user = 'service_role'
     OR current_user = 'supabase_admin'
     OR current_user = 'postgres'
  THEN
    RETURN NEW;
  END IF;

  IF TG_OP = 'INSERT' AND NEW.price IS NOT NULL AND NEW.price != 0 THEN
    RAISE EXCEPTION 'Price cannot be set by non-service roles (jwt=%, pg=%, sess=%, cur=%). Use server-side pricing.', jwt_role, pg_role, sess_role, current_user;
  END IF;
  IF TG_OP = 'UPDATE' AND NEW.price IS DISTINCT FROM OLD.price THEN
    RAISE EXCEPTION 'Price cannot be modified by non-service roles.';
  END IF;
  RETURN NEW;
END;
```

### `enforce_driver_fee_integrity()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** none
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
  jwt_role text := current_setting('request.jwt.claim.role', true);
  pg_role text := current_setting('role', true);
  sess_role text := session_user;
BEGIN
  IF jwt_role = 'service_role' 
     OR pg_role = 'service_role' 
     OR pg_role = 'supabase_admin' 
     OR pg_role = 'postgres'
     OR sess_role = 'service_role'
     OR sess_role = 'supabase_admin'
     OR sess_role = 'postgres'
     OR current_user = 'service_role'
     OR current_user = 'supabase_admin'
     OR current_user = 'postgres'
  THEN
    RETURN NEW;
  END IF;

  IF TG_OP = 'INSERT' AND NEW.young_driver_fee IS NOT NULL AND NEW.young_driver_fee != 0 THEN
    RAISE EXCEPTION 'Young driver fee cannot be set by non-service roles (jwt=%, pg=%, sess=%, cur=%). Use server-side pricing.', jwt_role, pg_role, sess_role, current_user;
  END IF;
  IF TG_OP = 'UPDATE' AND NEW.young_driver_fee IS DISTINCT FROM OLD.young_driver_fee THEN
    RAISE EXCEPTION 'Young driver fee cannot be modified by non-service roles.';
  END IF;
  RETURN NEW;
END;
```

### `generate_booking_code()`

- **Returns:** `text`
- **Language:** plpgsql
- **Security:** SECURITY INVOKER
- **Tables read:** none
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
  chars TEXT := 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789';
  result TEXT := '';
  i INTEGER;
BEGIN
  FOR i IN 1..8 LOOP
    result := result || substr(chars, floor(random() * length(chars) + 1)::integer, 1);
  END LOOP;
  RETURN result;
END;
```

### `generate_invoice_number()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY INVOKER
- **Tables read:** none
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
BEGIN
  IF NEW.invoice_number IS NULL OR NEW.invoice_number = '' THEN
    NEW.invoice_number := 'INV-' || TO_CHAR(NOW(), 'YYYY') || '-' || LPAD(nextval('final_invoice_seq')::TEXT, 5, '0');
  END IF;
  RETURN NEW;
END;
```

### `generate_member_id()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** none
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
BEGIN
    IF NEW.member_id IS NULL THEN
        NEW.member_id := 'MBR-' || UPPER(SUBSTRING(MD5(NEW.id::TEXT || now()::TEXT) FROM 1 FOR 8));
        NEW.membership_joined_at := now();
    END IF;
    RETURN NEW;
END;
```

### `generate_ticket_id()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** `support_tickets_v2`
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
  v_candidate text;
  v_tries int := 0;
BEGIN
  LOOP
    v_candidate := 'TKT-' || LPAD(nextval('public.support_ticket_seq')::text, 6, '0');
    EXIT WHEN NOT EXISTS (SELECT 1 FROM public.support_tickets_v2 WHERE ticket_id = v_candidate);
    v_tries := v_tries + 1;
    IF v_tries > 10000 THEN
      RAISE EXCEPTION 'Unable to allocate a unique ticket number';
    END IF;
  END LOOP;
  NEW.ticket_id := v_candidate;
  RETURN NEW;
END;
```

### `get_available_categories(p_location_id uuid)`

- **Returns:** `TABLE(id uuid, name text, description text, image_url text, daily_rate numeric, seats integer, fuel_type text, transmission text, available_count bigint)`
- **Language:** sql
- **Security:** SECURITY DEFINER
- **Tables read:** `vehicle_categories`, `vehicle_units`
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
SELECT 
    vc.id,
    vc.name,
    vc.description,
    vc.image_url,
    vc.daily_rate,
    vc.seats,
    vc.fuel_type,
    vc.transmission,
    COUNT(vu.id) FILTER (WHERE vu.status = 'available') as available_count
  FROM vehicle_categories vc
  LEFT JOIN vehicle_units vu ON vu.category_id = vc.id AND vu.location_id = p_location_id
  WHERE vc.is_active = true
  GROUP BY vc.id, vc.name, vc.description, vc.image_url, vc.daily_rate, vc.seats, vc.fuel_type, vc.transmission
  ORDER BY vc.sort_order ASC, vc.name ASC;
```

### `get_category_availability(p_location_id uuid, p_start_at timestamp with time zone, p_end_at timestamp with time zone, p_exclude_hold uuid DEFAULT NULL::uuid, p_exclude_booking uuid DEFAULT NULL::uuid)`

- **Returns:** `TABLE(id uuid, name text, description text, image_url text, daily_rate numeric, seats integer, fuel_type text, transmission text, sort_order integer, total_count bigint, available_count bigint)`
- **Language:** sql
- **Security:** SECURITY DEFINER
- **Tables read:** `bookings`, `reservation_holds`, `vehicle_categories`, `vehicle_units`
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
WITH win AS (
    SELECT
      p_start_at - interval '30 minutes' AS w_start,
      p_end_at   + interval '30 minutes' AS w_end
  ),
  -- Units physically usable at this location
  cap AS (
    SELECT vu.category_id, COUNT(*)::bigint AS units
    FROM vehicle_units vu
    WHERE vu.category_id IS NOT NULL
      AND vu.location_id = p_location_id
      AND LOWER(COALESCE(vu.status, '')) NOT IN
          ('maintenance', 'damage', 'damaged', 'retired', 'inactive', 'out_of_service', 'sold')
      AND (
        vu.is_temporary = false
        OR (
          (vu.temp_start_date IS NULL OR vu.temp_start_date <= (SELECT w_start FROM win)::date)
          AND (vu.temp_end_date IS NULL OR vu.temp_end_date >= (SELECT w_end FROM win)::date)
        )
      )
    GROUP BY vu.category_id
  ),
  -- Bookings that consume a unit of a category at this location during the window
  demand AS (
    SELECT
      COALESCE(
        b.internal_unit_category_id,
        (SELECT vu2.category_id FROM vehicle_units vu2 WHERE vu2.id = b.assigned_unit_id),
        (SELECT vc.id FROM vehicle_categories vc WHERE vc.id = b.vehicle_id)
      ) AS category_id,
      COUNT(*)::bigint AS used
    FROM bookings b, win
    WHERE b.status IN ('pending', 'confirmed', 'active')
      AND (p_exclude_booking IS NULL OR b.id <> p_exclude_booking)
      AND b.start_at < win.w_end
      AND b.end_at   > win.w_start
      AND COALESCE(
            (SELECT vu3.location_id FROM vehicle_units vu3 WHERE vu3.id = b.assigned_unit_id),
            b.location_id
          ) = p_location_id
    GROUP BY 1
  ),
  -- Live checkout holds
  held AS (
    SELECT
      COALESCE(
        rh.category_id,
        (SELECT vc.id FROM vehicle_categories vc WHERE vc.id = rh.vehicle_id)
      ) AS category_id,
      COUNT(*)::bigint AS used
    FROM reservation_holds rh, win
    WHERE rh.status = 'active'
      AND rh.expires_at > now()
      AND (p_exclude_hold IS NULL OR rh.id <> p_exclude_hold)
      AND rh.start_at < win.w_end
      AND rh.end_at   > win.w_start
      AND (rh.location_id IS NULL OR rh.location_id = p_location_id)
    GROUP BY 1
  )
  SELECT
    c.id,
    c.name,
    c.description,
    c.image_url,
    c.daily_rate,
    c.seats,
    c.fuel_type,
    c.transmission,
    c.sort_order,
    COALESCE(cap.units, 0) AS total_count,
    GREATEST(
      COALESCE(cap.units, 0) - COALESCE(d.used, 0) - COALESCE(h.used, 0),
      0
    )::bigint AS available_count
  FROM vehicle_categories c
  LEFT JOIN cap    ON cap.category_id = c.id
  LEFT JOIN demand d ON d.category_id = c.id
  LEFT JOIN held   h ON h.category_id = c.id
  WHERE c.is_active = true
  ORDER BY c.sort_order ASC, c.name ASC;
```

### `has_role(_user_id uuid, _role app_role)`

- **Returns:** `boolean`
- **Language:** sql
- **Security:** SECURITY DEFINER
- **Tables read:** `user_roles`
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
SELECT EXISTS (
    SELECT 1
    FROM public.user_roles
    WHERE user_id = _user_id
      AND role = _role
  )
```

### `is_active_staff(_user_id uuid)`

- **Returns:** `boolean`
- **Language:** sql
- **Security:** SECURITY DEFINER
- **Tables read:** `staff_assignments`
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
SELECT public.is_super_admin(_user_id)
     OR EXISTS (
       SELECT 1 FROM public.staff_assignments
       WHERE user_id = _user_id AND is_active = true
     )
```

### `is_admin_or_staff(_user_id uuid)`

- **Returns:** `boolean`
- **Language:** sql
- **Security:** SECURITY DEFINER
- **Tables read:** `user_roles`
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
SELECT EXISTS (
    SELECT 1
    FROM public.user_roles
    WHERE user_id = _user_id
      AND role IN ('super_admin', 'manager', 'admin', 'staff', 'cleaner', 'finance', 'support')
  )
```

### `is_super_admin(_user_id uuid)`

- **Returns:** `boolean`
- **Language:** sql
- **Security:** SECURITY DEFINER
- **Tables read:** `user_roles`
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
SELECT EXISTS (
    SELECT 1 FROM public.user_roles
    WHERE user_id = _user_id AND role IN ('super_admin', 'admin')
  )
```

### `is_support_or_admin(_user_id uuid)`

- **Returns:** `boolean`
- **Language:** sql
- **Security:** SECURITY DEFINER
- **Tables read:** `user_roles`
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
SELECT EXISTS (
    SELECT 1
    FROM public.user_roles
    WHERE user_id = _user_id
      AND role IN ('super_admin', 'manager', 'admin', 'staff', 'support')
  )
```

### `log_delivery_status_change()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** none
- **Tables written:** `delivery_status_log`

**Body (verbatim — every statement in execution order):**

```sql
BEGIN
  -- Log every status change to the history table
  IF OLD.status IS DISTINCT FROM NEW.status THEN
    INSERT INTO public.delivery_status_log (
      booking_id,
      status,
      notes,
      photo_urls,
      location_lat,
      location_lng,
      created_by
    ) VALUES (
      NEW.booking_id,
      NEW.status,
      NEW.notes,
      NEW.photo_urls,
      NEW.location_lat,
      NEW.location_lng,
      NEW.updated_by
    );
  END IF;
  RETURN NEW;
END;
```

### `log_deposit_status_change()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** none
- **Tables written:** `audit_logs`

**Body (verbatim — every statement in execution order):**

```sql
BEGIN
  IF OLD.deposit_status IS DISTINCT FROM NEW.deposit_status THEN
    INSERT INTO public.audit_logs (
      action,
      entity_type,
      entity_id,
      old_data,
      new_data
    ) VALUES (
      'deposit_status_change',
      'booking',
      NEW.id,
      jsonb_build_object('deposit_status', OLD.deposit_status),
      jsonb_build_object('deposit_status', NEW.deposit_status)
    );
  END IF;
  RETURN NEW;
END;
```

### `log_return_step_completion()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** none
- **Tables written:** `audit_logs`

**Body (verbatim — every statement in execution order):**

```sql
BEGIN
  -- Log when return state changes
  IF OLD.return_state IS DISTINCT FROM NEW.return_state THEN
    INSERT INTO public.audit_logs (
      action,
      entity_type,
      entity_id,
      user_id,
      old_data,
      new_data
    ) VALUES (
      'return_step_change',
      'booking',
      NEW.id,
      COALESCE(NEW.return_intake_completed_by, NEW.return_evidence_completed_by, NEW.return_issues_reviewed_by, auth.uid()),
      jsonb_build_object('return_state', OLD.return_state),
      jsonb_build_object('return_state', NEW.return_state)
    );
  END IF;
  RETURN NEW;
END;
```

### `notify_branch_on_new_ticket()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** none
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
  v_base text := 'https://bsvsveoaihtbsteqikvp.supabase.co/functions/v1/';
  v_key text := 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImJzdnN2ZW9haWh0YnN0ZXFpa3ZwIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjcxMzU4NzgsImV4cCI6MjA4MjcxMTg3OH0._xvISKl-E5Sv4V_al_QTncuGoSJ9WrRuYNwWu4UzKOY';
  v_headers jsonb;
BEGIN
  v_headers := jsonb_build_object(
    'Content-Type', 'application/json',
    'Authorization', 'Bearer ' || v_key
  );

  -- Branch alert (existing behaviour, unchanged)
  BEGIN
    PERFORM net.http_post(
      url := v_base || 'notify-branch-sms',
      headers := v_headers,
      body := jsonb_build_object('type', 'ticket', 'ticketId', NEW.id)
    );
  EXCEPTION WHEN OTHERS THEN
    RAISE WARNING 'notify_branch_on_new_ticket branch alert failed for %: %', NEW.id, SQLERRM;
  END;

  -- Customer confirmation, customer-raised tickets only. Independent block so a
  -- failure here can never affect the branch alert or the ticket insert.
  IF NEW.created_by_type = 'customer' THEN
    BEGIN
      PERFORM net.http_post(
        url := v_base || 'send-support-sms',
        headers := v_headers,
        body := jsonb_build_object('ticketId', NEW.id, 'kind', 'created')
      );
    EXCEPTION WHEN OTHERS THEN
      RAISE WARNING 'notify_branch_on_new_ticket customer confirmation failed for %: %', NEW.id, SQLERRM;
    END;
  END IF;

  RETURN NEW;
END;
```

### `recompute_invoice_totals(p_booking_id uuid)`

- **Returns:** `void`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** `bookings`, `final_invoices`, `payments`
- **Tables written:** `final_invoices`

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
  v_payments numeric := 0;
  v_offline numeric := 0;
  v_grand numeric := 0;
  v_paid_offline boolean := false;
  v_total_amount numeric := 0;
  v_new_due numeric := 0;
  v_new_status text;
  v_current_status text;
BEGIN
  -- Sum of live payment rows (exclude deposits, only completed/captured)
  SELECT COALESCE(SUM(amount), 0) INTO v_payments
  FROM public.payments
  WHERE booking_id = p_booking_id
    AND status IN ('completed', 'captured')
    AND COALESCE(payment_type, '') <> 'deposit';

  -- Offline (bank transfer) paid: credit the invoice grand_total
  SELECT COALESCE(paid_offline, false), COALESCE(total_amount, 0)
    INTO v_paid_offline, v_total_amount
  FROM public.bookings
  WHERE id = p_booking_id;

  -- Update every invoice for this booking
  FOR v_grand, v_current_status IN
    SELECT grand_total, status FROM public.final_invoices WHERE booking_id = p_booking_id
  LOOP
    v_offline := CASE WHEN v_paid_offline THEN GREATEST(v_grand - v_payments, 0) ELSE 0 END;
    v_new_due := GREATEST(v_grand - v_payments - v_offline, 0);
    v_new_status := CASE
      WHEN v_new_due <= 0.005 AND v_current_status = 'issued' THEN 'paid'
      WHEN v_new_due > 0.005 AND v_current_status = 'paid' THEN 'issued'
      ELSE v_current_status
    END;

    UPDATE public.final_invoices
    SET payments_received = v_payments,
        paid_offline_amount = v_offline,
        amount_due = v_new_due,
        status = v_new_status,
        updated_at = now()
    WHERE booking_id = p_booking_id;
  END LOOP;
END;
```

### `release_vin_from_booking(p_booking_id uuid, p_new_status text DEFAULT 'available'::text)`

- **Returns:** `void`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** `bookings`
- **Tables written:** `vehicle_units`

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
  v_unit_id UUID;
BEGIN
  SELECT assigned_unit_id INTO v_unit_id
  FROM bookings
  WHERE id = p_booking_id;
  
  IF v_unit_id IS NOT NULL THEN
    UPDATE vehicle_units
    SET status = p_new_status,
        updated_at = now()
    WHERE id = v_unit_id;
  END IF;
END;
```

### `set_booking_code()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY INVOKER
- **Tables read:** none
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
BEGIN
  IF NEW.booking_code IS NULL OR NEW.booking_code = '' THEN
    NEW.booking_code := generate_booking_code();
  END IF;
  RETURN NEW;
END;
```

### `set_first_response_time()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY INVOKER
- **Tables read:** none
- **Tables written:** `support_tickets_v2`

**Body (verbatim — every statement in execution order):**

```sql
BEGIN
  IF NEW.sender_type = 'staff' AND NEW.message_type = 'customer_visible' THEN
    UPDATE public.support_tickets_v2
    SET first_response_at = COALESCE(first_response_at, now()),
        updated_at = now()
    WHERE id = NEW.ticket_id;
  END IF;
  RETURN NEW;
END;
```

### `staff_location(_user_id uuid)`

- **Returns:** `uuid`
- **Language:** sql
- **Security:** SECURITY DEFINER
- **Tables read:** `staff_assignments`
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
SELECT location_id FROM public.staff_assignments
  WHERE user_id = _user_id AND is_active = true
  LIMIT 1
```

### `trg_recompute_invoice_on_booking_offline()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** none
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
BEGIN
  IF NEW.paid_offline IS DISTINCT FROM OLD.paid_offline
     OR NEW.total_amount IS DISTINCT FROM OLD.total_amount THEN
    PERFORM public.recompute_invoice_totals(NEW.id);
  END IF;
  RETURN NEW;
END;
```

### `trg_recompute_invoice_on_insert()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** none
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
BEGIN
  PERFORM public.recompute_invoice_totals(NEW.booking_id);
  RETURN NEW;
END;
```

### `trg_recompute_invoice_on_payment()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** none
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
  v_booking_id uuid;
BEGIN
  v_booking_id := COALESCE(NEW.booking_id, OLD.booking_id);
  IF v_booking_id IS NOT NULL THEN
    PERFORM public.recompute_invoice_totals(v_booking_id);
  END IF;
  RETURN NULL;
END;
```

### `update_delivery_status_on_booking_change()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** `delivery_statuses`
- **Tables written:** `delivery_statuses`

**Body (verbatim — every statement in execution order):**

```sql
BEGIN
  -- If pickup_address was just added (booking converted to delivery)
  IF OLD.pickup_address IS NULL AND NEW.pickup_address IS NOT NULL THEN
    IF NOT EXISTS (SELECT 1 FROM public.delivery_statuses WHERE booking_id = NEW.id) THEN
      INSERT INTO public.delivery_statuses (
        booking_id,
        status,
        updated_by,
        created_at,
        updated_at
      ) VALUES (
        NEW.id,
        CASE WHEN NEW.assigned_driver_id IS NOT NULL THEN 'assigned' ELSE 'unassigned' END,
        NEW.assigned_driver_id,
        now(),
        now()
      );
    END IF;
  END IF;
  
  -- If driver was just assigned, update status from unassigned to assigned
  IF OLD.assigned_driver_id IS NULL AND NEW.assigned_driver_id IS NOT NULL THEN
    UPDATE public.delivery_statuses 
    SET status = 'assigned', 
        updated_by = NEW.assigned_driver_id,
        updated_at = now()
    WHERE booking_id = NEW.id AND status = 'unassigned';
  END IF;
  
  -- If driver was removed, set status back to unassigned
  IF OLD.assigned_driver_id IS NOT NULL AND NEW.assigned_driver_id IS NULL THEN
    UPDATE public.delivery_statuses 
    SET status = 'unassigned', 
        updated_by = NULL,
        updated_at = now()
    WHERE booking_id = NEW.id AND status = 'assigned';
  END IF;
  
  RETURN NEW;
END;
```

### `update_points_balance(p_user_id uuid, p_points integer, p_booking_id uuid DEFAULT NULL::uuid, p_transaction_type points_transaction_type DEFAULT 'earn'::points_transaction_type, p_money_value numeric DEFAULT NULL::numeric, p_notes text DEFAULT NULL::text, p_created_by uuid DEFAULT NULL::uuid, p_expires_at timestamp with time zone DEFAULT NULL::timestamp with time zone)`

- **Returns:** `TABLE(new_balance integer, ledger_id uuid)`
- **Language:** plpgsql
- **Security:** SECURITY DEFINER
- **Tables read:** `profiles`
- **Tables written:** `points_ledger`, `profiles`

**Body (verbatim — every statement in execution order):**

```sql
DECLARE
    v_current_balance INTEGER;
    v_new_balance INTEGER;
    v_ledger_id UUID;
BEGIN
    -- Lock the row for update to prevent race conditions
    SELECT points_balance INTO v_current_balance
    FROM profiles
    WHERE id = p_user_id
    FOR UPDATE;
    
    IF v_current_balance IS NULL THEN
        RAISE EXCEPTION 'User not found';
    END IF;
    
    v_new_balance := v_current_balance + p_points;
    
    -- Prevent negative balance
    IF v_new_balance < 0 THEN
        RAISE EXCEPTION 'Insufficient points balance';
    END IF;
    
    -- Update balance
    UPDATE profiles
    SET points_balance = v_new_balance,
        updated_at = now()
    WHERE id = p_user_id;
    
    -- Insert ledger entry
    INSERT INTO points_ledger (
        user_id, booking_id, transaction_type, points, 
        money_value, balance_after, notes, created_by, expires_at
    )
    VALUES (
        p_user_id, p_booking_id, p_transaction_type, p_points,
        p_money_value, v_new_balance, p_notes, p_created_by, p_expires_at
    )
    RETURNING id INTO v_ledger_id;
    
    RETURN QUERY SELECT v_new_balance, v_ledger_id;
END;
```

### `update_updated_at_column()`

- **Returns:** `trigger`
- **Language:** plpgsql
- **Security:** SECURITY INVOKER
- **Tables read:** none
- **Tables written:** none

**Body (verbatim — every statement in execution order):**

```sql
BEGIN
  NEW.updated_at = now();
  RETURN NEW;
END;
```

---

## 6. Triggers

| Table | Trigger | Timing | Event | Level | Function |
| --- | --- | --- | --- | --- | --- |
| `abandoned_carts` | `update_abandoned_carts_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `booking_add_ons` | `trg_enforce_addon_price` | BEFORE | INSERT OR UPDATE | ROW | `enforce_addon_price_integrity()` |
| `booking_additional_drivers` | `trg_enforce_driver_fee` | BEFORE | INSERT OR UPDATE | ROW | `enforce_driver_fee_integrity()` |
| `booking_extensions` | `update_booking_extensions_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `bookings` | `log_booking_deposit_status` | AFTER | UPDATE | ROW | `log_deposit_status_change()` |
| `bookings` | `recompute_invoice_on_booking_offline` | AFTER | UPDATE | ROW | `trg_recompute_invoice_on_booking_offline()` |
| `bookings` | `trg_block_sensitive_booking_inserts` | BEFORE | INSERT | ROW | `block_sensitive_booking_inserts()` |
| `bookings` | `trg_block_sensitive_booking_updates` | BEFORE | UPDATE | ROW | `block_sensitive_booking_updates()` |
| `bookings` | `trigger_create_delivery_status` | AFTER | INSERT | ROW | `create_delivery_status_for_booking()` |
| `bookings` | `trigger_log_return_step` | AFTER | UPDATE | ROW | `log_return_step_completion()` |
| `bookings` | `trigger_set_booking_code` | BEFORE | INSERT | ROW | `set_booking_code()` |
| `bookings` | `trigger_update_delivery_status` | AFTER | UPDATE | ROW | `update_delivery_status_on_booking_change()` |
| `bookings` | `update_bookings_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `checkin_records` | `update_checkin_records_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `competitor_pricing` | `update_competitor_pricing_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `customers` | `update_customers_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `damage_reports` | `trigger_auto_create_damage_ticket` | AFTER | INSERT | ROW | `auto_create_damage_ticket()` |
| `delivery_statuses` | `trigger_log_delivery_status_change` | AFTER | UPDATE | ROW | `log_delivery_status_change()` |
| `delivery_statuses` | `update_delivery_statuses_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `delivery_tasks` | `update_delivery_tasks_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `deposit_jobs` | `update_deposit_jobs_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `final_invoices` | `recompute_invoice_after_insert` | AFTER | INSERT | ROW | `trg_recompute_invoice_on_insert()` |
| `final_invoices` | `set_invoice_number` | BEFORE | INSERT | ROW | `generate_invoice_number()` |
| `final_invoices` | `update_final_invoices_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `fleet_cost_cache` | `update_fleet_cost_cache_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `incident_cases` | `trigger_auto_create_incident_ticket` | AFTER | INSERT | ROW | `auto_create_incident_ticket()` |
| `incident_cases` | `update_incident_cases_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `incident_repairs` | `update_incident_repairs_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `locations` | `update_locations_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `maintenance_logs` | `update_maintenance_logs_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `payment_profiles` | `update_payment_profiles_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `payments` | `recompute_invoice_on_payment` | AFTER | INSERT OR DELETE OR UPDATE | ROW | `trg_recompute_invoice_on_payment()` |
| `profiles` | `trigger_generate_member_id` | BEFORE | INSERT OR UPDATE | ROW | `generate_member_id()` |
| `profiles` | `update_profiles_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `receipts` | `update_receipts_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `rental_agreements` | `update_rental_agreements_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `staff_assignments` | `update_staff_assignments_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `support_tickets_v2` | `set_ticket_id` | BEFORE | INSERT | ROW | `generate_ticket_id()` |
| `support_tickets_v2` | `trg_alert_on_new_ticket` | AFTER | INSERT | ROW | `create_alert_for_new_ticket()` |
| `support_tickets_v2` | `trg_branch_sms_on_new_ticket` | AFTER | INSERT | ROW | `notify_branch_on_new_ticket()` |
| `ticket_messages_v2` | `reopen_on_customer_reply` | AFTER | INSERT | ROW | `auto_reopen_ticket_on_reply()` |
| `ticket_messages_v2` | `track_first_response` | AFTER | INSERT | ROW | `set_first_response_time()` |
| `tickets` | `update_tickets_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `vehicle_categories` | `update_vehicle_categories_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `vehicle_expenses` | `update_vehicle_expenses_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `vehicle_units` | `update_vehicle_units_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `vehicles` | `update_vehicles_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `vendors` | `update_vendors_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |
| `walkaround_inspections` | `update_walkaround_inspections_updated_at` | BEFORE | UPDATE | ROW | `update_updated_at_column()` |

Purpose of each trigger function is documented in section 5 (function body).

---

## 7. Dead schema

Method: literal search (`rg -F`) for each table name (quoted, and as
`from('<table>')`) and for each column name across `src/`,
`supabase/functions/` and `e2e/`. A zero count means no application code
mentions the identifier anywhere, including generated types usage.

### 7.1 Tables with no code references

| Table | Code references |
| --- | --- |
| `incident_repairs` | 0 |
| `rate_limits` | 0 |
| `webhook_events` | 0 |

### 7.2 Columns with no code references

Columns are grouped by table. Columns named generically (`id`, `created_at`,
`updated_at`, `status`, `notes`) are excluded because a literal match would
be meaningless.


GAP: unreferenced columns are still written by database defaults or
triggers in some cases (for example audit columns), so a zero code
reference means "no application code reads or writes it", not "always
null".

### 7.3 Legacy / superseded objects still present

| Object | Evidence |
| --- | --- |
| `tickets`, `ticket_messages` | superseded by `support_tickets_v2` / `ticket_messages_v2`; `check_ticket_escalation()` (section 5) still reads and writes the legacy `tickets` table while the app writes v2. |
| `vehicles` | superseded by `vehicle_categories` + `vehicle_units`; `get_category_availability()` still falls back to `bookings.vehicle_id` joined against `vehicle_categories`. |
| `webhook_events` | payment webhook idempotency store; see `supabase/functions/wl-webhook`. |

